# PT-Plugin-Plus 隐私政策

生效日期：2026-08-25

PT-Plugin-Plus 是在用户浏览器中运行的开源浏览器扩展。本项目不运营遥测、广告、分析或数据中转服务器，不出售用户数据，也不会将用户数据提供给项目维护者。

## 处理的数据

扩展可能在浏览器本地保存以下数据：

- 扩展界面、搜索、刷新、下载和备份设置；
- 用户添加的 PT 站点、下载器、保存目录、标签和 WebDAV/备份服务配置；
- 站点 Cookies、用户名、用户统计历史和未读状态；
- 搜索结果快照、下载历史、收藏/分组和辅种任务；
- 规范化影片编号，以及可重新获取的影片资料、评分和海报缓存；
- 下载器、站点或备份服务的地址、用户名、密码、令牌或其他凭据；
- 运行状态、重试计划以及已经脱敏的失败原因。

这些数据默认保存在 `chrome.storage.local`、IndexedDB 或用户主动创建的本地备份文件中。扩展不会把这些数据发送给项目维护者。

## 数据发送范围

扩展只会为用户请求的功能连接以下目标：

- 用户已经添加或访问的 PT 站点，用于搜索、读取用户信息、获取种子和执行站点页面增强；
- 用户配置的 qBittorrent、Transmission 或其他兼容下载服务，用于连接测试和推送种子；
- 用户配置的 WebDAV 或备份服务，用于连接测试、上传和恢复备份；
- 豆瓣、IMDb 或兼容影片资料来源，用于用户启用或触发的影片候选、评分与资料查询；
- 用户主动打开的项目主页、归档文档或问题反馈页面。

第三方站点和服务对其收到的数据适用各自的隐私政策。用户应只配置自己信任的服务。

## 搜索与影片资料缓存

普通 PT 搜索的关键词、候选列表、当前影片绑定和完整种子结果只保留在当前扩展页面内存中。搜索参数完成一次页面内路由交接后会从地址栏移除；扩展不会把普通搜索写入 `sessionStorage`、`chrome.storage.local` 或 IndexedDB。页面内导航可以继续访问当前结果，完整刷新或关闭页面后不会自动恢复。

用户主动创建搜索快照时，关键词、搜索参数、影片身份和完整结果会写入 `chrome.storage.local`，并可在用户选择该备份字段时进入本地 ZIP 或 WebDAV 备份。用户可以单独删除、清空或不备份搜索快照。

影片候选按原始查询建立的短期缓存只存在于扩展进程内存。启用影片资料缓存时，IndexedDB 只保存 IMDb、豆瓣、TMDb、TVMaze 等规范编号，以及可重新获取的标题、主创、简介、评分和海报；不保存普通搜索词、候选列表、种子结果、失败查询历史或 provider 凭据。默认资料有效期为 7 天、评分为 24 小时、失败重试抑制为 15 分钟；30 天未再次使用的影片会删除，且最多保留 200 部。用户可以修改这些限制、完全禁用缓存、查看数量/占用并一键清空。影片资料缓存不会进入 ZIP、WebDAV 或 Chrome Sync 备份。

搜索和影片 provider 的诊断只记录站点或来源标识、结果数量和脱敏错误码，不记录原始关键词、完整请求 URL、完整快照或种子结果。

## 浏览器权限用途

| 权限 | 用途 |
| --- | --- |
| `alarms` | 在 Manifest V3 service worker 可能被回收时，恢复自动刷新、延迟下载、备份和失败重试。 |
| `clipboardWrite` | 在用户点击复制操作时写入种子链接或搜索结果。 |
| `contextMenus` | 提供用户可关闭的选中文字搜索、影片链接搜索和下载链接推送入口。 |
| `cookies` | 读取站点登录状态、执行用户选择的 Cookies 备份/恢复和有效期延长。 |
| `downloads` | 在用户选择本地保存时下载种子文件或备份文件。 |
| `declarativeNetRequest` | 为部分站点请求临时设置必要请求头；规则只在当前浏览器会话中使用。 |
| `storage`、`unlimitedStorage` | 保存配置、用户历史、快照、收藏、下载记录和其他本地数据。 |
| `notifications` | 通知后台批量下载或持久任务的完成与失败。 |
| `offscreen` | 在 Chrome MV3 中完成 DOM 解析、剪贴板和备份等 service worker 不具备的能力。 |
| `*://*/*` host access | 支持用户访问的 PT 站点，以及地址无法在构建期预知的本地下载器、WebDAV 和备份服务。内容脚本只在识别到受支持站点或用户启用的社交站点后挂载功能。 |

扩展不申请 `activeTab` 权限。

## 备份与加密

用户可以选择备份的数据域。备份可能包含 Cookies、下载器或 WebDAV 凭据等敏感信息，请只保存到可信位置。

启用 AES 加密时，加密密钥只保存在当前浏览器中，不写入备份文件。遗失密钥后，项目维护者无法恢复加密备份。未启用加密的备份由用户自行负责安全保存。

每个远端备份服务可以独立启用旧备份自动清理，默认关闭。扩展使用仅保存在当前浏览器中的验证密钥识别当前服务创建的新自动备份；该密钥、待处理清理记录和恢复租约不会写入备份文件。自动清理只处理通过验证且符合用户所选年龄、数量或分层保留规则的自动备份，并始终保留配置的最低份数。启用后，自动备份成功会自行预检并清理合格候选，不要求用户逐次确认。

手动备份、旧版未分类备份、其他应用文件、无法验证的文件和正在恢复的备份不会被自动删除。用户可以通过历史页面的手动清理预览查看候选、取消选择并进行二次确认，也可以仅在当次预览中纳入严格匹配的旧版备份；这不会永久授予自动删除旧版文件的权限。逐文件删除结果和脱敏失败原因会保存在本地运行历史中。已经从第三方服务删除的远端文件能否恢复取决于该服务自身的回收站或版本策略。

## 保留、删除与控制

除上述普通搜索内存和有界影片缓存外，数据会保留到用户在扩展中删除、清空或覆盖相应记录，或者通过 Chrome 删除扩展数据。用户可以关闭站点工具栏、右键菜单、自动刷新、自动备份、自动清理、Cookies 延期、影片候选和影片资料缓存等功能，也可以删除下载器、站点、备份服务及其历史。

卸载扩展通常会删除 Chrome 为该扩展保存的本地数据；用户已经导出的本地文件或上传到第三方服务的备份需要由用户自行删除。

## 安全与问题反馈

扩展会尽量避免在可见日志和下载历史错误中显示 Cookies、认证头、passkey、令牌和密码。没有任何软件能够保证绝对安全，请勿导入来源不明的备份，也不要向他人发送包含敏感数据的日志或备份。

隐私或安全问题可通过[项目问题反馈](https://github.com/JohnathanT55/PT-Plugin-Plus/issues)提交；公开反馈前请先移除凭据和个人数据。
