# Changelog

本文件记录 PT-Plugin-Plus Manifest V3 延续版本的主要变化。

## 未发布

### 修复与说明

- 修复早期 MV3 版本创建的 WebDAV 备份服务可能缺少内部 ID，导致编辑窗口为空，以及立即备份、历史和删除操作无法正确定位配置的问题；升级后会以现有字典键幂等修复记录，原地址、凭据和备份选择保持不变。
- 编辑备份服务时不再依赖弹窗进场动画加载配置，并在深拷贝 Pinia 响应式数据前解除 Proxy，保证后台标签页、恢复后的标签页和普通前台打开均能可靠显示及保存。
- 将分层保留字段明确为“近期直接保留份数”“周度代表覆盖周数”和“月度代表覆盖月数”：周/月数表示最多覆盖的不同日历周期，每个周期只额外保护一份代表，并非每周或每月保留多份。
- 明确自动与手动清理的区别：启用保留策略后，自动备份成功会自行预检并清理合格候选；历史页的手动清理预览用于审核候选和一次性处理旧版未分类备份。

## 2.1.0 — 2026-08-25

### 已完成

- 新增 PTPP 风格的“我的下载器”，支持汇总状态、筛选、刷新、暂停、继续、删除及磁力/种子文件推送。
- 新增按备份流隔离的年龄、数量和分层保留策略，以及严格分类、清理预览、二次确认和浏览器重启恢复；自动清理默认关闭并保护手动、旧版及无法验证的文件。
- 放宽站点常驻工具栏的宽度、按钮间距和紧凑视窗回退，同时保持拖动、停靠和菜单方向一致。
- 搜索结果使用可横向滚动的次要列和固定操作列；其他表格保持各自原生布局，辅种及下载历史在窄窗口优先保证操作可达。
- 恢复 PTPP 风格的搜索结果影片聚合头卡；显式候选、直接 ID 和安全消歧的普通文字搜索共享规范影片身份，搜索快照能够完整恢复该身份。
- 聚合豆瓣/PTGen、IMDb、可选 TMDb/OMDb 与 TVMaze 的字段级资料和独立评分，支持单源失败、按来源重试、过期数据先显示后刷新和电影/剧集差异。
- 影片资料 IndexedDB 缓存增加独立资料/评分有效期、短期失败抑制、请求去重、30 天硬保留、200 部上限、禁用、统计和显式清空；普通搜索词、候选和种子结果不再进入会话或长期存储。
- 增加仓库隐私审计，阻止私人网络地址、本机用户路径、具体测试目录和常见高置信度密钥格式进入提交。

## 2.0.0 — 2026-08-13

### 新增与恢复

- 将 background、options、content script 和 DOM 能力升级到 Manifest V3 service worker/offscreen 架构。
- 接入当前构建导入的 PTD 站点定义；各站点按自身定义提供搜索、用户数据、列表/详情工具栏和下载入口，不使用固定站点白名单。
- 恢复 PTPP 的站点独立下载器、目录和标签优先级，并支持 qBittorrent 与 Transmission。
- 恢复下载历史、搜索快照、收藏/分组、辅种、影片候选和用户数据统计。
- 支持本地与 WebDAV 备份、AES 加密、备份历史、失败重试、Cookies 延期以及用户数据合并/覆盖恢复。
- 支持普通恢复入口直接导入归档版 PTPP ZIP，并迁移敏感凭据、Cookies 与全部支持的数据域。
- 站点工具栏增加全局左/右停靠设置、相对视窗位置、旧绝对坐标迁移和双向菜单。

### 界面与范围

- 以归档版 PTPP 的导航、宽屏表格、固定操作栏和常驻站点工具栏为界面基准。
- 常规设置拆分为界面、站点工具栏、浏览器集成、用户数据、搜索、下载、备份七个职责单一的标签。
- 移除生产导航中的 PTD 独有媒体服务器、原生通信桥和调试器入口。
- 修正当前版本、归档文档和迁移 FAQ 的仓库链接；产品界面不直接展示个人仓库 slug。

### 安全与验证

- 移除未使用的 `activeTab` 权限。
- 禁止动态远端代码、`unsafe-eval`、`eval()` 和 `new Function()` 进入生产构建。
- 增加版本化存储、幂等迁移、持久任务恢复、凭据脱敏和构建产物验证。
- 在真实 Chrome 环境完成 MV3 扩展页、service worker、站点页面、下载器、WebDAV、主要 UI 和浏览器重启闭环。
- 使用 Chrome Stable 原生加载发布候选，并完成生产入口、实际弹窗、亮暗主题、小窗口、键盘可访问性和真实站点工具栏验收。
- 增加 Chrome Web Store 静态合规审计、可复现 ZIP/SHA-256 打包和真实 Windows 休眠生命周期探针。
- 持久一次性任务增加跨执行上下文互斥、领取状态校验与超时租约恢复，避免休眠/唤醒边界重复执行。
- 修复恢复较新版本备份时取消版本警告会锁住恢复窗口的问题。
- 完成真实 Windows Modern Standby/唤醒验收；持久一次性任务在唤醒边界严格执行一次并恢复原配置。
