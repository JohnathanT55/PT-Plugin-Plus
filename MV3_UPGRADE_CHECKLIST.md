# PT-Plugin-Plus Manifest V3 首发与第二批更新清单

更新日期：2026-08-25

依据：`usable_function/`、本仓库 `legacy-mv2/` 中的归档版 PTPP、[pt-plugins/PT-Plugin-Plus](https://github.com/pt-plugins/PT-Plugin-Plus)，以及 PT-depiler 中与 PTPP 原有功能重合的实现。

状态说明：

- `[x]`：对应阶段范围内已经实现，并通过自动验证或对应的 Chrome 实机检查。
- `[ ]`：已经进入当前阶段范围、仍需实现或验收。
- `[外部]`：不属于源码实现，可在发布提交后由维护者完成的商店或平台门禁。
- Chrome 是当前首发目标；Edge 和 Firefox 的未完成项不阻塞 Chrome 首发。

## 1. 产品范围

- **归档版 PTPP 是产品功能、导航、交互和视觉样式的基准。**
- PT-depiler 只复用 PTPP 已有同类功能的 MV3 底层、站点定义和下载器适配器，不继承 PTD 独有的产品外壳与功能入口。
- 保留 PTPP 的核心差异能力：按站点绑定下载服务器、保存目录和标签，并能一次点击直接推送。
- 不恢复任意动态 JS 站点/搜索插件；MV3 只接受静态模块和可验证、可序列化的请求配置。
- 首发必须保留用户现有站点、下载器、站点目录映射、用户历史、搜索快照、收藏、辅种、下载历史、Cookies 和 WebDAV 设置。
- 所有随构建导入的 PTD 站点定义使用同一套产品能力与权限规则；只针对各定义声明的页面和响应方式做必要兼容，不另设站点级产品限制。

### 1.1 明确不进入 Chrome 首发的内容

| 功能                                  | 决定                                                        |
| ------------------------------------- | ----------------------------------------------------------- |
| 自定义语言包导入/替换                 | 首版移除，以后可作为纯数据功能恢复。                        |
| Chrome storage.sync 旧式直接备份      | 移除，由本地备份和 WebDAV 取代。                            |
| GoodMoviesList 页面增强、封面模式     | 首版移除，有明确需求再恢复。                                |
| `reseed.tongyifan.me` 页面增强        | 首版移除。                                                  |
| 任意自定义站点 JS/CSS、可执行搜索脚本 | 移除；不符合 MV3 静态代码要求。                             |
| 独立 schema/权限设置页                | 移除；改用源码类型、构建期注册和按需权限。                  |
| OWSS                                  | 不阻塞首发。                                                |
| PTD 媒体服务器、原生通信桥、Ask AI    | PTPP 原版没有，不显示入口。                                 |
| PTD“我的下载器”任务总览               | 不进入 2.0.0 首发；已纳入第 9 节第二批更新。                |
| TMDb                                  | 不进入 2.0.0 首发；第二批仅作为影片资料后备 provider 使用。 |
| PTD 调试器                            | 不默认恢复；系统日志和版本提示已经保留。                    |
| Edge / Firefox                        | Chrome 首发后单独安排。                                     |

## 2. 首发功能状态

### 2.1 MV3 架构与构建

- [x] 根项目采用 Manifest V3；旧 MV2 项目完整保存在 `legacy-mv2/`，Chrome 产物输出到 `dist-chrome/`。
- [x] Chrome background 改为 service worker，监听器在顶层同步注册；DOMParser、剪贴板等能力通过幂等 offscreen document 提供。
- [x] 建立类型化消息、版本化数据模型、结构校验、分域 storage envelope、不可变 revision 分片和显式迁移注册表。
- [x] PTPP 运行时迁移已经升级到 bridge v4；storage 与 IndexedDB 全部成功后才提交完成标记，失败可安全重试。
- [x] 权限拆分为 `permissions`、`optional_permissions` 和 `host_permissions`；Web Accessible Resources 使用 MV3 对象格式。
- [x] 生产构建不含 `unsafe-eval`、`eval()`、`new Function()`、远端脚本或旧动态站点/下载器代码。
- [x] Vue 3、Vite、Vuetify、Pinia、options UI、content script、service worker 和 offscreen document 已进入生产构建。

### 2.2 站点与搜索

- [x] 站点定义层直接注册并构建当前导入的 PTD 站点模块，不以固定站点白名单限制可添加站点；具体搜索、用户信息和页面能力以各站点定义声明为准。
- [x] 聚合搜索、实时用户信息、列表/详情工具栏和下载入口使用统一站点接口；代表性真实站点已完成实机回归，单站失败不会阻塞其他站点。
- [x] 站点可独立启停搜索和用户信息，维护 URL、名称、分组、排序、超时与必要凭据；域名变化使用稳定 `siteId`。
- [x] 聚合/单站搜索、搜索方案、分类、标签、排序、筛选、优惠过滤和最近搜索词可用。
- [x] 搜索结果支持批量复制、本地下载、远程推送、创建快照、收藏和辅种。
- [x] 全新搜索会清除上一轮筛选和排队任务，并以搜索代际隔离已经发出的旧请求。
- [x] 每页结果限制为 10/25/50，旧 100/“全部”设置自动降至 50；已减少大结果集的重复监听和状态读取。
- [x] 搜索快照支持创建、加载、重命名、删除和清空；删除后选中状态与按钮即时复位。
- [x] 搜索快照已经进入本地/WebDAV备份，当前 MV3 加密恢复实测后快照数量保持一致。
- [x] 支持 PTD #475 的数据化搜索方案自定义，但不执行用户脚本。
- [x] 搜索框恢复老板 PTPP 的影片候选：支持中文/英文关键词、IMDb/豆瓣 ID、候选数量、按 ID/标题搜索，并复用 PTD 影片资料、评分和缓存；编号模式优先使用兼容站点更多的 IMDb ID，无 IMDb 时回退豆瓣 ID。
- [x] 候选查询具备请求去重、TTL 缓存和失败回退；外部服务失败不会阻止直接搜索。

### 2.3 我的数据

- [x] 一键刷新全部或选中站点；刷新期间在每站“状态”列显示 PTPP 原版绿色转圈进度，完成后恢复最终状态。
- [x] 支持批量打开：有选择时打开所选站点，没有选择时打开全部可用站点。
- [x] 恢复 PTPP 一行一站表格、完整用户字段、筛选、排序、分页、列显示、历史时间线和统计图。
- [x] 现有等级要求、下一等级及积分预计能力继续保留；“在表格中显示下一等级要求”等等级展示选项默认开启，不再作为第二批重复开发项目。
- [x] 可配置每日刷新时间、失败重试、搜索/用户信息并发数及推荐范围提示。
- [x] 自动刷新、失败重试和刷新后上传使用 alarms/持久任务；service worker 回收与完整浏览器重启后可恢复。
- [x] Windows 系统休眠唤醒后继续执行且不重复刷新；生命周期验收见 5.2。

### 2.4 下载服务器与站点目录

- [x] qBittorrent 和 Transmission 的名称、启停、地址、认证、超时、连接测试、编辑和删除可用。
- [x] 下载服务器支持 `sortIndex` 编辑与表格排序；不要求额外拖拽排序。
- [x] 可设置全局默认下载服务器。
- [x] 每个“站点 + 下载服务器”支持多个候选目录/标签，并可指定默认服务器、目录、标签和自动开始策略。
- [x] 站点目录与对应下载服务器是不可拆分的目标，优先于全局默认下载服务器。
- [x] 统一解析规则用于搜索结果、站点列表、详情页、批量操作、右键菜单和历史重新推送。
- [x] 回退顺序为：站点显式目标 → 唯一站点绑定 → 全局默认/下载器默认行为 → 显示选择菜单。
- [x] “推送到默认下载器”无歧义时一次点击完成；“推送到……”显示站点专用目标后再列通用目标。
- [x] 下载前可看到服务器、目录和标签；下载历史保存实际使用的目标。
- [x] 多个真实站点已经用暂停任务完成独立目录推送；跨站批量推送也已确认不会串目录。

### 2.5 下载设置与下载历史

- [x] 记录来源、标题、下载器、目录、标签、结果、脱敏错误和时间，并支持筛选。
- [x] 支持重新下载、重新推送、删除选中和清空；操作后选择状态立即复位。
- [x] 失败重试、延迟、站点间隔和后台批次使用持久化任务，不依赖 service worker `sleep`。
- [x] 前台/后台批量、总体积超限确认、后台完成通知和浏览器重启恢复已经验证。
- [x] Windows 系统休眠唤醒后到期下载/重试只执行一次；生命周期验收见 5.2。

### 2.6 收藏与影片资料

- [x] 搜索结果和站点页面可添加/移除收藏，收藏列表支持筛选、排序、编辑、删除和清空。
- [x] 支持分组新增、改名、改色、删除、默认分组及收藏移入/移出；计数和失效引用自动修复。
- [x] 收藏页、行操作、分组卡、选中高亮和固定工具栏已经恢复 PTPP 的布局与语义。
- [x] 收藏可重新搜索、本地保存或推送，并沿用站点绑定下载器/目录优先级。
- [x] 收藏、分组和默认分组进入本地/WebDAV 备份、当前 MV3 恢复和旧版 `collection.json` 导入。
- [x] IMDb/豆瓣 ID 格式校验、资料补全、搜索语法和失败不阻塞保存已经接入。
- [x] 收藏和搜索共用 PTD 社交信息缓存，影片信息、评分及外部服务失败回退的真实网络闭环已完成。

### 2.7 辅种任务

- [x] 可从搜索结果创建辅种任务，保存来源、标题、大小、创建时间和校验状态。
- [x] 校验 torrent 文件信息，支持发送全部/选中项目、删除、清空、更新、备份和恢复。
- [x] 保存路径跟随基准种子并允许手动修改；切换基准种子会同步更新路径。
- [x] 暗色/浅色显示、操作按钮布局及关闭自动开始提醒已恢复并验收。

### 2.8 WebDAV、本地备份与恢复

- [x] WebDAV 服务的添加、编辑、删除、连接测试、手动备份、历史、恢复和远端删除可用。
- [x] 可选择配置、站点、下载器、站点目录、用户数据、搜索快照、下载历史、收藏、辅种和 Cookies。
- [x] AES 加密开关与随机密钥可用；密钥只保存在当前浏览器，不写入备份或日志。
- [x] `Digest` 已明确为 HTTP Digest 认证，不与备份加密混淆。
- [x] 固定间隔备份与“整轮用户刷新后上传”是两套独立设置，均使用 alarms/持久任务。
- [x] 失败状态、脱敏原因、重试时间、下次计划及最近 30 次运行详情可见。
- [x] 恢复 Cookies 时可延长有效期；用户数据恢复可选择合并或覆盖，覆盖前二次确认。
- [x] 真实 WebDAV 已完成明文/加密上传、错误/正确密钥、恢复、断网重试、worker 回收和完整浏览器重启闭环。
- [x] 刷新后自动上传仅在整轮成功或失败重试耗尽后触发一次，不在手动刷新单个站点时额外上传。
- [x] Windows 系统休眠唤醒后固定备份和失败重试不丢失、不重复；生命周期验收见 5.2。

### 2.9 内容工具栏与快速推送

- [x] 站点详情使用 PTPP 原版常驻纵向工具栏，不需要先点击 PTD 球形按钮。
- [x] 工具栏包含 Logo、一键下载、下载到/推送到、复制链接、收藏、快捷搜索和默认下载器空间。
- [x] 支持拖动、双击重置、视窗边界修正和拖入链接推送。
- [x] 默认推送就地展示执行中/成功/失败；选择其他目标使用按钮旁菜单而非 PTD 居中大对话框。
- [x] 内容脚本设置提供全局唯一且清晰可见的“左侧/右侧”停靠选项，默认右侧；首页、普通页、无种子页、种子列表和种子详情页均服从同一设置，不允许站点级左右例外。真实 Chrome 与实际站点页面已逐项验证默认右侧。
- [x] 工具栏位置保存全局停靠侧、相对边缘距离和纵向比例；旧绝对坐标统一迁移到右侧。已实测跨站切页、窄视窗缩放与恢复、浏览器重启、双击复位和旧版左侧绝对坐标迁移；无种子首页只显示 Logo 时仍保持右侧。
- [x] 后台/非活动标签页初始零视窗及未加载样式的瞬态全宽回归已补齐：使用真实可视视窗、页面与工具栏 `ResizeObserver` 主动重新计算，拒绝瞬态超宽测量；后台站点页面恢复后正确落在设定侧，设置页切换左右时已打开页面无需刷新即可同步。
- [x] 右侧停靠时菜单向左展开，左侧停靠时菜单向右展开；双击复位到所选侧的垂直中点，拖动和边界修正不会使工具栏或菜单跑出视窗。
- [x] 浏览器右键下载菜单同时跟随活动标签切换和同一标签页 URL 变化重建；导航离开 PT 站点后不会残留上一站点的绑定下载器、目录或标签，高级推送入口仍可用。真实 Chrome 已验证普通页面显示默认下载器，返回已配置站点后恢复站点默认下载器与独立保存目录；公开验收记录不保存实际目录。

### 2.10 Options 界面

- [x] 恢复黄色顶栏、深色左侧导航、分组、全局搜索框、当前搜索方案和主页/帮助入口。
- [x] 导航显隐、持久化、布局联动及圆形 hover/focus/pressed 反馈已经实机闭环。
- [x] 我的数据、搜索、快照、下载历史、收藏、辅种、下载服务器、站点、目录、搜索方案和备份页采用 PTPP 宽屏层级与术语。
- [x] 常规设置整理为职责单一的“界面与交互 / 站点工具栏 / 浏览器集成 / 用户数据 / 搜索 / 下载 / 备份与恢复”七个顶部标签；外观与表格行为、站点页面工具栏、浏览器右键菜单不再混放。
- [x] 页面顶栏、按钮、输入框、间距、禁用态和明暗主题已统一并逐页复核。
- [x] 发布候选可访问性与小窗口审计：全部生产入口及实际弹窗均有可读标题，图标操作有名称且可用键盘触发，关闭/展开等生成控件无匿名按钮；关闭后的弹窗不留在 DOM 或可访问性树中，外部链接统一使用安全 `rel`。真实 Chrome 的常规与小窗口验收均无页面级横向溢出或未处理异常；右键“高级推送”入口及暗色主题也已覆盖。
- [x] 成品入口审计：全部 24 个生产叶子入口、导航和七个职责单一的设置标签均已加载；包含不显示在导航中的 `/link-push`，生产界面不暴露 PTD 独有页面或个人 GitHub 用户名/仓库 slug。当前维护仓库、归档 PTPP 和 PTD 迁移 FAQ 分别使用各自正确链接；未引用的底层源码和语言字符串不要求为此删除。

## 3. 数据迁移与完整性

- [x] 可读取 `PT-Plugin-Plus-Config`，迁移已有站点、未知站点稳定 ID、下载器地址/认证和全局/站点默认目标。
- [x] 可迁移 `client.paths[host]` 中的全部目录及顺序、标签、自动刷新、重试、批量阈值和工具栏配置。
- [x] 可迁移用户历史、下载历史、搜索快照、收藏/分组、辅种任务及 WebDAV 设置。
- [x] 纯迁移函数可重复执行且结果确定；成功后不删除旧 key，日志不输出凭据。
- [x] 新扩展 ID 无法读取旧扩展 storage 时，普通恢复入口可识别 PTPP ZIP；独立 `userdatas.json` 和 `collection.json` 可按需导入。
- [x] 使用真实 MV2 完整备份完成最终导入回归，并逐项核对站点、下载器、目录、用户历史、快照、收藏/分组、辅种、下载历史、Cookies 和 WebDAV。
- [x] 迁移/恢复 UI 明确提示下载器、WebDAV、站点凭据和 Cookies 将被导入；数据只写入当前浏览器，日志和可见 UI 不显示原文。

说明：`PT-Plugin-Plus-Cache-Contents` 属于可再生成的运行缓存，不迁移；旧动态站点/下载器脚本保留在 MV2 归档或旧 key 中，但不会进入 MV3 可执行状态。

## 4. 最终验证摘要

- [x] `pnpm verify` 覆盖两套类型检查、业务测试、生产构建、MV3 产物检查、动态代码扫描和 worker runtime smoke。
- [x] 最终生产构建已在真实 Chrome 环境加载，Manifest V3、service worker 回收及完整浏览器重启均通过。
- [x] 影片候选、年份、别名、评分、数量限制、IMDb/豆瓣身份转换、ID/标题跳转、输入框显示及失败回退已完成真实网络验收；不支持特定身份类型的站点会正常跳过。
- [x] 多站点、两类下载器、独立站点目录和跨站批量推送已完成实机回归。
- [x] 搜索/快照、我的数据、下载历史、收藏、辅种、导航和主要设置页面完成 Chrome 交互回归。
- [x] WebDAV 手动、固定间隔、刷新后上传、加密恢复、断网重试和浏览器重启完成真实服务回归。
- [x] 老板版 `v1.6.1.2721` ZIP 已在干净浏览器环境中完整导入并经重启复核；站点、下载器、目录绑定、用户历史、下载历史、收藏/分组、辅种、WebDAV 与 Cookies 均完整保留并可用。公开验收记录不保存私人备份的精确库存数量。
- [x] 发布候选已覆盖全部生产入口、实际弹窗、常规/小窗口、亮暗主题和键盘可访问性；无页面级横向溢出、弹窗残留或 Options/service worker 未处理异常。
- [x] 原生右键菜单已在普通页面、实际站点页面和高级推送入口完成实机验收；站点上下文切换可正确恢复对应下载目标和工具栏位置。
- [x] Chrome Stable 已通过原生“加载已解压扩展”入口加载最终构建；Manifest V3、service worker、offscreen document、备份恢复、完整页面与站点工具栏均正常。
- [x] 最终发布 ZIP 解压后重新加载并再次通过运行时身份、完整页面和真实站点工具栏验收，证明测试对象是发布包而非仅工作目录。
- [x] Windows 休眠唤醒、Chrome Stable 和可自动化的商店合规均已完成，详见 5.2。

## 5. Chrome 首发完成项与外部门禁

### 5.1 功能闭环

- [x] 影片搜索闭环：IMDb/豆瓣资料、评分、关键词候选、候选数量、缓存和失败回退。
- [x] 内容工具栏全局左/右停靠设置：默认右侧且不允许站点级例外；所有站点及首页、普通页、无种子页、列表页、详情页一致；设置页提供可见且可保存的左/右选项；保存全局停靠侧、相对边缘距离和纵向比例，并迁移旧绝对坐标；菜单按停靠侧向内展开。真实 Chrome 与代表性站点已覆盖页面类型、后台恢复、窄视窗、即时同步、菜单方向和旧配置迁移。
- [x] 右键下载菜单活动标签同步：监听标签激活及当前标签 URL 更新，避免站点默认下载目标跨页面残留；已实测普通页面显示高级推送、全局默认下载器与通用下载器目标，且全局默认目标不冒充站点配置、不显示空括号；返回已配置站点后恢复该站点绑定目标。

### 5.2 发布验收

- [x] Windows 系统休眠唤醒生命周期测试。一次超过 5 分钟及一次约 20 分钟的真实 Modern Standby 均由系统电源事件确认；唤醒后到期 task/alarm 均清除，注入的 `Retry #1` 开始/完成日志各且仅有一条，0 站点、0 失败，普通周期刷新因未到间隔明确跳过，探针随后恢复原配置。普通等待或模拟 alarm 未被计作证据，公开验收记录不保存本机事件时间。
- [x] 设置标签职责收口：新增独立“站点工具栏”和“浏览器集成”标签，移动现有设置且保持旧路由兼容、保存行为和配置数据不变。
- [x] 弹窗/反馈、小窗口和可访问性统一审计：已补齐弹窗名称、图标按钮名称与键盘入口、安全外链属性；全部 24 个生产叶子入口和 14 类弹窗在常规及 900×700 两轮实测中无页面级溢出或关闭残留，并完成右键高级推送的亮暗主题与窄视窗布局回归。
- [x] 成品可见名称、路由与入口越界审计：当前版本提交/发布、归档版 PTPP 文档和 PTD FAQ 使用各自正确仓库；界面只显示“项目仓库/问题反馈”等产品文案，不直接展示个人 GitHub 用户名或仓库 slug；生产导航不暴露 PTD 独有页面，隐藏的高级推送入口也已单独纳入审计。
- [x] Chrome 稳定版加载：已通过原生文件夹选择器加载 `PT-Plugin-Plus 2.0.0`，并完成有数据页面与站点工具栏实机验收。
- [x] Chrome Web Store 静态合规：当前全部成品文件通过 MV3、权限、CSP、远端脚本/动态代码、四组精确尺寸图标、双语本地化和隐私政策逐项映射检查；发布 ZIP 根目录、文件数、manifest 和 SHA-256 经过二次读取验证。
- [x] 持久一次性任务的实机对照探针发现并修复重复领取竞态：领取必须处于 `scheduled` 状态，Chrome 执行上下文使用 Web Locks 互斥，并以 30 分钟租约恢复中断任务；跨 coordinator 重入和过期租约均有自动回归。
- [x] 最终备份恢复回归覆盖“备份版本高于当前版本”的原生确认框；取消后会复位恢复状态，不再因同步对话框悬挂或依赖人工预置路由。
- [外部] Chrome Web Store 实际上传、商店资料填写及 Google 人工审核。这是发布账户与商店状态门禁，不能由本地测试伪造为已通过。
- [x] 商店权限代码与文档收口：成品 manifest 不含无调用依据的 `activeTab`；Cookies、下载、通知、右键菜单、离屏文档、动态请求头、持久存储和全站 host access 已在隐私政策中逐项说明用途，并明确项目不收集或出售用户数据。实际 Web Store 提交与审查仍保留在上一项。
- [x] 最终版本号确认为 `2.0.0`；README、`CHANGELOG.md`、`PRIVACY.md` 与打包说明已收口。`pnpm package:chrome` 可复现生成 `PT-Plugin-Plus-v2.0.0-chrome.zip` 和 SHA-256 文件，连续两次生成的摘要一致。

## 6. 首发后范围决定（不阻塞 Chrome 2.0.0）

- [x] Edge 不属于 Chrome 2.0.0 首发完成定义；首发后建立独立 profile 回归，当前不加载以避免和用户旧版冲突。
- [x] Firefox 当前不承诺支持；若以后决定支持，再建立独立兼容 background 构建与验收清单。
- [x] 2.0.0 保留现有系统日志和版本提示，不恢复 PTD 调试器；只有实际需求出现后再增强。
- [x] 1.1 中移除或延期的 P2 功能不回溯进入 2.0.0；第二批只实现第 9 节已经明确接受的项目，其余项目仍不扩张范围。

## 7. 首发完成定义

Chrome MV3 首发需要同时满足：

1. 当前导入的 PTD 站点定义均可被添加，并按各自能力声明提供搜索、用户信息、页面工具栏和下载入口。
2. qBittorrent、Transmission 及每站独立下载目录可靠，不会因全局默认设置而串目录。
3. 下载历史、收藏/分组、搜索快照、辅种和用户历史无数据损失。
4. 本地/WebDAV 手动与自动备份可成功创建并恢复。
5. 自动刷新、下载/重试和备份任务在 service worker 回收、浏览器重启及系统休眠后可靠运行。
6. 真实 MV2 备份可以完整导入，并向用户清楚说明敏感凭据处理方式。
7. 生产界面符合 PTPP 产品范围，Chrome 稳定版和商店合规检查通过。

## 8. 首发收口记录（已完成）

### A. 数据完整性收口（已完成）

1. 用真实 MV2 ZIP 完成一次完整导入。
2. 逐项对照站点、下载器、站点目录、用户历史、快照、收藏/分组、辅种、下载历史和 Cookies。
3. 补齐凭据导入提示和相关自动回归。

已用用户提供的真实老板版 ZIP 完成实现修复、干净 profile 导入、逐域核对和完整浏览器重启复核。

### B. PTPP 影片搜索收口（已完成）

1. 完成 IMDb/豆瓣影片资料与评分展示。
2. 完成关键词候选和候选数量设置。
3. 验证缓存、超时、失败回退以及搜索/收藏两处 UI。

已完成影片候选、资料与评分、数量和点击模式设置、缓存与失败回退，并通过真实 Chrome 网络闭环验收。首发范围内已基本没有明显业务功能缺口。

### C. 发布候选验收（已完成）

1. [x] 设置页提供可见的工具栏全局左/右停靠入口，默认右侧；已修复个别站点在首页/无种子页、列表页和详情页错误落到左侧的问题。真实 Chrome 与代表性站点已完成页面类型、旧坐标迁移、后台/非活动标签恢复、跨站切页、缩放、浏览器重启、设置即时同步及菜单展开方向验收。
2. [x] 将站点工具栏、浏览器集成从界面外观设置中拆分为独立标签，并完成全部 24 个生产叶子入口（含隐藏高级推送入口）的成品可见性、暗色主题、小窗口和可访问性审计。
3. [x] Chrome Stable 原生加载、Web Store 静态合规及最终发布 ZIP 实战验收。
4. [x] 零外部副作用探针已完成真实 Windows Modern Standby/唤醒验收；源码侧首发完成定义全部满足。后续只剩维护者上传 Chrome Web Store 并等待外部人工审核。

A、B、C 以及本机全部技术门禁均已完成；Chrome Stable、最终 ZIP、备份恢复、真实站点工具栏与 Windows Modern Standby 均有实战证据。剩余 Chrome Web Store 上传与 Google 人工审核属于提交后的外部门禁。

## 9. 第二批更新清单（已完成，不影响 Chrome 2.0.0 首发结论）

本批 9.1–9.6 六项均已完成并通过对应的自动验证与 Chrome 实机验收。实现继续以归档版 PTPP 的导航、交互和视觉风格为产品基准；PTD 只提供可复用的 MV3 数据结构、下载器适配器、站点模块和计算实现，没有随之引入媒体服务器、原生通信桥或其他 PTD 独有入口。

### 9.1 新增“我的下载器”标签页（已完成）

实施前代码审查结论：本仓库 `app/src/entries/options/views/Overview/MyClient/` 与当时本地 PTD 实现一致，下载器查询、状态、暂停、恢复和删除消息也已存在于 offscreen 层；2.0.0 只是按首发范围没有注册该路由。因此第二批在这些底层上恢复入口并改造成 PTPP 风格，没有直接暴露 PTD 产品外壳。

- [x] 在“概览”导航组新增“我的下载器”标签和独立路由；页面标题、固定工具栏、按钮高度、表格密度、选中高亮、分页及亮暗主题与现有 PTPP 页面一致。
- [x] 首批只管理当前产品支持且已启用的 qBittorrent、Transmission；不因新增页面顺带开放其他下载器、媒体服务器或原生通信桥。
- [x] 汇总所有启用下载器的连接状态、客户端版本、地址、种子数、当前上传/下载速度及累计流量；支持按下载器筛选，并保证单个下载器断线不阻塞其他下载器。
- [x] 展示种子所属下载器、名称、标签、大小、进度、状态、上传/下载速度、累计上传/下载、分享率、保存路径和添加时间；支持按名称、info hash、标签及路径搜索，并可配置列、排序和每页数量。
- [x] 支持手动刷新、进入页面时自动加载开关和可配置的前台自动刷新；同一下载器连续失败三次后只暂停该下载器并给出明确原因，用户可单独恢复，不创建依赖常驻 MV3 service worker 的计时器。
- [x] 支持单条和批量暂停、继续、删除；删除必须二次确认，并把“同时删除数据”作为默认关闭的独立选项。操作结束后立即刷新受影响下载器并清空失效选择状态。
- [x] 支持粘贴一个或多个种子 URL、上传一个或多个 `.torrent` 文件并推送；已识别站点的 URL 必须继续服从 PTPP 的“站点绑定下载器 + 保存目录 + 标签”优先级，不能绕过现有目标解析与下载历史。
- [x] 当前界面不提供“查看原始 JSON”；下载器返回、批量错误和下载历史在跨扩展边界及持久化前均进行可序列化与敏感 URL 脱敏处理。
- [x] 后台消息返回统一的成功/失败结果，批量操作展示逐下载器统计和脱敏错误；刷新、暂停、继续、删除、推送期间不得锁死页面或留下已关闭弹窗。
- [x] 下载器协议闭环已覆盖状态、版本、速度、流量、筛选、手动/自动刷新、失败隔离、暂停、继续、两种删除、磁力/文件推送、站点目录优先级、配置恢复及浏览器重启。
- [x] 最终构建已使用真实 qBittorrent 与 Transmission 完成同范围实机复核；测试只操作专用任务，不影响既有任务，且公开验收记录不保存私人端点、凭据或本机路径。

验收证据：最终 MV3 构建已在真实 Chrome 与两类真实下载客户端中完成状态查询、任务管理、推送、站点目录优先级、自动刷新、配置恢复和浏览器重启闭环；运行时无异常，测试结束后用户配置与历史保持完整。

### 9.2 自动备份保留、标记与安全清理（已完成）

实施前代码审查结论：WebDAV/OpenList 后端已经支持列出远端 ZIP、读取时间/大小和逐文件删除，备份历史页面也支持手动多选删除；但当时手动备份、固定间隔备份和“整轮用户数据刷新后上传”的文件都使用 `PTPP_backup_时间.zip`，单凭远端文件名无法可靠区分来源。因此自动清理先为新文件增加可验证的来源/备份流标记，没有直接按旧文件名或整个 WebDAV 目录的 ZIP 数量删除。

- [x] 为新备份写入稳定的 `backupId`、创建时间、触发来源（手动/固定间隔/用户数据刷新）、完整或选定字段范围及范围指纹；清理所需的非敏感分类同时进入文件名并以当前服务器本地密钥的截断 HMAC-SHA-256 验证，使远端列表无需逐个下载解密即可判断，完整元数据仍写入 manifest。
- [x] 只把符合严格 PTPP 新命名规则、且属于当前备份服务器对应备份流的文件纳入自动保留规则；手动备份、其他应用的 ZIP、名称相似文件、临时上传文件和无法验证的文件永不自动删除。
- [x] 在每个备份服务器中提供独立的“自动清理旧备份”开关，默认关闭；把“删除早于 N 天的自动备份，同时至少保留 M 份”作为最直接的默认策略，并提供“只保留最近 N 份”。分层保留（最近若干份、每周一份、每月一份）作为可选高级策略，不强迫用户使用。
- [x] 固定间隔的选定字段备份与刷新后上传的完整备份必须按不同备份流分别计算保留，不允许一份局部备份占用完整灾难恢复备份的保留名额；字段选择变化后使用新的范围指纹，旧范围不会被误当成同一备份流。
- [x] 每次成功上传并重新列出远端文件后才计算清理候选；始终保护刚上传且已验证的备份、每个备份流配置的最少保留份数（默认至少 3 份）及所有手动备份。上传失败、列表失败、时间无法解析或加密状态未知时不执行自动删除。
- [x] 备份历史页面增加“自动/手动/旧版未分类”和“保留/待清理”标记，并提供清理预览：显示将保留、将删除的文件数量、日期范围与预计释放空间；用户可从候选中取消选择，再通过二次确认执行“立即清理”。
- [x] 现有 `PTPP_backup_时间.zip` 统一标为“旧版未分类”，默认只允许手动选择删除；可提供一次性的“按年龄/数量纳入本次清理”选项，但必须先展示完整候选列表并二次确认，不能把该选择永久扩展为对所有未分类 ZIP 的自动删除权限。
- [x] 自动删除采用受限并发并逐文件记录结果；部分删除失败不回滚已经成功的删除，也不把已经成功的新备份改记为上传失败，而是在备份运行详情中单独记录清理成功数、失败数和脱敏原因，下一轮可安全重试。
- [x] 清理任务必须与同一服务器的上传串行化并保持幂等；浏览器关闭、service worker 回收或网络中断后不得重复删除，也不得删除刚被另一轮上传创建或正在恢复的文件。
- [x] 为命名/分类、分层取样、简单 N 份、多个备份流、夏令时/月周边界、重复时间、伪造文件名、列表失败和部分删除失败补齐纯函数与消息测试；在专用 OpenList 测试目录放入自动、手动、旧版和其他应用 ZIP，完成预览、真实删除、重启恢复及“无越界删除”Chrome 实机验收。

验收证据：完整自动验证、生产构建与 Chrome Web Store 静态审计均通过。真实 Chrome 和 OpenList/WebDAV 环境已覆盖多备份流、手动/自动备份、严格文件分类、清理预览与取消、选择性删除、重启恢复、自动保留及越界保护；测试仅使用专用临时子目录，结束后删除测试数据并恢复原配置。公开记录不保存私人端点、凭据、服务器标识或随机目录名。

### 9.3 放宽站点常驻工具栏布局（已完成）

实施前代码审查结论：站点小浮窗原先固定为 `80px` 宽，每个操作按钮固定 `50px` 高，但图标已经达到 `34px`，按钮内边距仅 `3px 4px`、标签行高为 `1.25`，图标、文字和分隔线因而显得拥挤；`App.vue` 的未加载样式回退宽度也写死为 `80px`。本次调整因此同时覆盖视觉尺寸和停靠坐标计算，而非只扩大外层 CSS。

- [x] 将工具栏适度加宽到约 `92–100px`，按钮使用至少 `56–60px` 的稳定高度和更充足的上下/左右内边距；图标、标签、分隔线之间保留清晰空隙，同时保持单列紧凑而不变成大面板。
- [x] 调整图标与文字比例、标签行高和换行规则；“一键下载本页”“推送到……”“本地下载”“复制链接”“高级列表”“快捷搜索”等完整文案应易读，不得被图标挤压、意外裁切或在中英文切换后横向溢出。
- [x] Logo、顶部拖动条、状态颜色、悬停/键盘焦点和禁用态随新宽度统一对齐；每个操作保留至少 `44×44px` 的可点击区域，按钮间分隔线宽度与两侧留白一致。
- [x] 把工具栏基准宽度收口为共享常量或 CSS 变量，位置测量尚未就绪时也使用新宽度；同步更新右侧/左侧停靠、拖动、双击复位、边界夹取、旧坐标迁移和 ResizeObserver 逻辑，避免扩大后重新跳到左侧或超出视窗。
- [x] 下载目标菜单继续朝页面内侧展开，并以对应按钮为锚点；工具栏加宽后菜单不得遮住工具栏、跑出视窗或改变目标选择逻辑。
- [x] 对低高度和窄宽度视窗提供紧凑回退或内部可访问滚动，确保全部操作仍能到达；普通桌面视窗使用放宽后的默认样式，不因响应式规则又退回当前拥挤布局。
- [x] 在代表性真实站点的首页、列表页和详情页复核不同按钮组合，并覆盖左右停靠、亮暗网页背景、常用页面缩放、普通及窄视窗；自动测试同步校验新尺寸下无横向溢出、无停靠偏移且菜单方向正确。

验收证据：真实 Chrome 与代表性站点页面已覆盖首页、列表、详情、左右停靠、亮暗背景、中英文、页面缩放、普通/窄视窗、菜单方向、拖动复位、旧坐标迁移、后台恢复和设置即时同步。默认与紧凑布局均无裁切、横向溢出、停靠越界或运行时异常；站点工具栏能力由统一内容脚本提供，不设置个别站点例外。

### 9.4 搜索结果影片信息聚合头卡（已完成）

实施与代码审查结论：归档版在搜索页顶部通过 `MovieInfoCard` 展示豆瓣中文资料，并通过 OMDb 补充 IMDb、Rotten Tomatoes 和 Metacritic 评分，但依赖已经失效或不应共享的旧接口与密钥。当前 MV3 已将候选身份、多来源字段聚合、有界缓存和 PTPP 宽屏头卡收口为独立闭环，并明确普通搜索与用户主动快照的不同隐私边界。

- [x] 以 IMDb ID 作为优先规范身份，同时保存豆瓣、TMDb 等外部 ID、所选标题和年份；显式候选、直接 ID、安全消歧的普通文字搜索及搜索快照恢复都能指向同一影片，不能只保存最终的 `imdb|tt...` 搜索词。普通搜索身份只在页面内存保留，页面内导航不丢失；完整刷新按隐私边界主动清除，只有用户创建的快照能够跨刷新恢复。
- [x] 普通文字搜索不阻塞 PT 搜索；候选已经唯一、标题与年份足够明确或用户显式选择候选时才绑定影片身份。存在多个同名条目时继续展示候选供用户选择，不为追求显示头卡而静默展示错误影片。
- [x] 建立规范化影片实体，覆盖中文/原始标题、别名、年份、地区、类型、导演、编剧、演员、上映日期、片长、简介、海报、外部 ID 以及豆瓣、IMDb、Rotten Tomatoes、Metacritic 等相互独立的评分；每个字段记录来源和更新时间。
- [x] provider 按职责而非整包覆盖：豆瓣/PTGen 优先提供中文资料与豆瓣评分，TMDb 官方 API 作为完整资料和海报后备，IMDb 静态评分接口提供 IMDb 评分，OMDb 仅在用户配置自己的 Key 后补充 Rotten Tomatoes、Metacritic 和基础资料，TVMaze 只补充剧集。
- [x] 采用字段级合并和 `Promise.allSettled` 并行请求；任一 provider 失败只能缺少对应字段或评分，不能使整张卡消失，也不能阻塞或取消 PT 站点搜索。所有评分保留来源标签，不混算为单一分数。
- [x] PTGen provider 链去重后依次尝试用户配置入口、GitHub Pages、OurHelp CDN 和 OurHelp API；豆瓣海报使用代理请求、Blob/Data URL 内存缓存和候选 host 回退，避免防盗链导致卡片反复破图。
- [x] 使用 IndexedDB 按规范身份持久缓存可重新获取的影片实体，并建立外部 ID 别名索引；资料和评分使用独立 TTL，支持过期数据先显示后刷新、短期失败负缓存、同请求去重、单 provider 超时/重试及新旧搜索代际隔离。普通搜索词、候选、种子结果和选择时间不写入长期缓存；缓存可禁用、统计、清空，30 天未使用自动删除且最多 200 部，不进入任何备份。
- [x] 在搜索状态栏上方恢复归档版 PTPP 的宽屏影片头卡：固定加载骨架避免结果表跳动，展示标题、年份、主创、类型、日期、片长、简介及独立评分按钮；支持折叠、占位海报、部分成功态、缓存时间、失败来源提示和按 provider 重试，亮暗主题均沿用 PTPP 风格。
- [x] “搜索时显示影片信息”与“影片候选”作为独立开关并默认开启；TMDb/OMDb 凭据放入高级 provider 设置，不内置共享密钥。没有配置可选密钥时仍可依靠零配置来源和已有缓存展示可取得的核心资料。
- [x] 为身份解析、候选消歧、字段优先级、外部 ID 合并、provider 部分失败、超时、负缓存、过期回显、请求代际、普通搜索隐私边界和快照恢复补齐自动测试；Chrome 实机覆盖多候选与年份消歧、普通文字安全绑定、直接 ID、电影/剧集、页面导航/刷新、显式快照恢复、断网旧缓存、单源重试、缓存禁用/保留/清空、亮暗主题和普通/窄视窗。

验收证据：完整自动测试、类型检查、生产构建、MV3 产物验证和 Chrome Web Store 静态审计均通过。真实 Chrome 环境确认运行中的扩展名称、版本与 Manifest V3 身份；候选、身份、头卡、缓存、失败回退、隐私边界、快照恢复、主题和响应式场景均通过，运行时错误与网络警告为 0。测试结束后恢复原扩展配置、搜索快照与 IndexedDB 数据。

### 9.5 搜索结果宽表格响应式与设置导航收口（已完成）

实施与代码审查结论：问题根源不是缺少应用级缩放，而是搜索结果宽表格需要同时容纳身份、可滚动详情和随时可达的操作区。上一版内部缩放还会同时放大 Vuetify overlay、固定定位和点击坐标的复杂度，因此已安全语义撤回提交 `4512a718` 中的全局缩放系统，并参考 PTD 提交 `15f9c4db` 的页面结构确认其同样不依赖内部全局缩放；保留 Chrome 原生缩放，`100%` 作为默认基线。普通表格沿用各页面原生 `v-data-table`，不套用搜索结果专用列角色。

- [x] 为搜索结果建立 `primary / secondary / action` 三类列契约：站点和标题作为主列随表格正常滚动；分类、大小、状态和时间等次要列保留最小宽度并可横向滚动；操作列固定在右侧且不改写用户的列显示、排序、分页或选择配置。
- [x] 操作表头和单元格使用独立堆叠层、左侧分隔阴影与完全不透明背景；普通、斑马纹、悬停、选中、选中悬停五种状态均不会让下方大小、时间或统计数字穿透到按钮区域，命中测试确认按钮/操作单元格始终位于次要列之前。
- [x] 搜索结果共享表格提供顶部可聚焦横向滚动条，并与 Vuetify 原生底部滚动条双向同步；`ResizeObserver`、`MutationObserver` 和滚动监听在重连/卸载时完整清理，导航开关、窗口变化、表头变化和增量搜索结果不会残留重复监听。初始宽度改用微任务测量，不依赖后台扩展页可能被 Chrome 节流的 `requestAnimationFrame`，失焦后重新进入搜索结果也能稳定显示滚动条。
- [x] 仅搜索结果使用响应式容器及明确列角色，并继续限制为 10/25/50 条；下载历史、收藏、辅种、我的数据、我的下载器、搜索快照、下载服务器、站点、下载目录、搜索方案、备份服务器/历史和日志全部恢复各自原生 `v-data-table` 展示。
- [x] 删除 PTPP 内部全局缩放控件、配置、备份字段、根级 zoom/scale CSS 及旧缩放测试，同时移除仅凭 `devicePixelRatio` 判断异常的紫色提示；Chrome 80%/100%/125% 原生缩放由搜索结果响应式布局和其他页面原生布局共同承担，不修改标签页缩放或增加权限。
- [x] 修复重复点击左侧“常规设置”后父路由取消子标签导致内容空白：菜单始终解析到默认子路由 `SetBaseGeneral`，非法/父路由立即归一化；设置页移除无配对的 `v-window`，重复点击当前左侧入口或顶部“界面与交互”标签均保持内容和保存栏可见。
- [x] 自动测试覆盖表头归一化、多操作键、显式宽度、滚动边界、实色隔离和设置默认子路由；真实 Chrome 已覆盖多种桌面视窗、原生缩放、导航开关、亮暗主题及中英文。搜索结果固定操作列、滚动同步和按钮命中均正常，其他表格保持原生布局，设置重复点击不会空白。

### 9.6 辅种任务与下载历史局部布局收口（已完成）

实施前代码审查结论：这两页不需要套用搜索结果的固定操作列。辅种任务的问题来自选择模型仍保存整行对象、标题列不主动让位以及操作/展开列总宽度过大；下载历史则是标题内容缺少可收缩边界。因此修复保持两页原生 `v-data-table` 的交互和外观，只在页面内明确关键列宽并让长标题截断。

- [x] 辅种任务的表格选择值统一为任务 ID；顶部“删除”逐个调用现有 `deleteKeepUploadTask` 消息，成功后同步移除存储记录、表格行和选择状态。单行删除与“清空全部”继续复用原有独立流程，未改变其确认语义。
- [x] 辅种任务在窄视窗中优先保留大小、种子数、创建时间、五个操作按钮和展开按钮；标题与保存路径主动截断/隐藏，不再依赖横向滚动才能触达最后一个操作。
- [x] “种子数”表头使用稳定的横向宽度和不换行标签；展开后的全部种子列表在站点图标与标题/上传下载数据之间保留 `12px` 间距，避免图标贴住内容。
- [x] 下载历史使用页面局部固定表格布局，站点、下载器、时间、状态和操作列保留稳定宽度，长标题在剩余空间中省略；操作按钮在常见桌面宽度下保持可见。
- [x] 真实 Chrome 已完成辅种任务选择、确认删除、持久化记录与表格行同步消失的闭环；行内操作和展开按钮可达，长标题正确让位，“种子数”横向显示，展开条目间距及下载历史标题截断均正常。
