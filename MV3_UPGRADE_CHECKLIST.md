# PT-Plugin-Plus Manifest V3 升级范围与验收清单

更新日期：2026-08-11
依据：`usable_function/` 中的实际使用截图、归档版 [pt-plugins/PT-Plugin-Plus](https://github.com/pt-plugins/PT-Plugin-Plus)（本仓库 `legacy-mv2/`），以及 PT-depiler 中与 PTPP 既有功能重合的实现。

状态说明：

- `[x]`：当前范围内已经实现，并通过自动验证或对应的 Chrome 实机检查。
- `[ ]`：仍缺少实现，或条目包含尚未完成的关键实机/生命周期验收；部分已有代码的条目会在末尾明确注明“代码已实现，待验收”。
- Chrome 是当前首发与实机验收目标；Edge/Firefox 的未完成项不阻塞 Chrome 首发。

## 1. 已确定的产品方向

- **归档版 PTPP 是产品功能、导航、交互和视觉样式的唯一基准。** PT-depiler 只可作为 PTPP 已有同类功能的 MV3 实现来源，不能因为已经导入 PTD 代码就把 PTD 独有功能或 PTD 的产品外壳加入本项目。
- 站点定义、站点请求和完整搜索的底层能力可复用 PT-depiler 的数据模型与实现，但功能入口、页面结构、术语和主要交互必须与 PTPP 对齐；不继续维护 PTPP 的动态 JS 站点插件体系。
- MV3 底层架构可参考 PT-depiler：Chrome service worker + offscreen document + 类型化消息 + alarms + storage/IndexedDB。架构复用不等于产品功能继承。
- 保留 PTPP 最有价值的差异能力：**按站点绑定下载服务器、保存目录和标签，并能快速直推**。
- 第一版只为实际使用的 PTPP 功能负责；未使用的旧功能不阻塞 MV3 首发，PTD 独有功能暂不实现也不显示入口。
- 配置迁移必须保留用户现有站点、下载器、站点目录映射、历史、收藏、快照和 WebDAV 设置。

### PT-depiler 可复用部分的边界

2026-08-10 核对了 PT-depiler 的[开发计划](https://github.com/orgs/pt-plugins/projects/1/views/1)和[迁移常见问题 #316](https://github.com/pt-plugins/PT-depiler/discussions/316)：

- PTD 已完成默认下载器、下载器候选目录/标签、快速推送和“记住上一次推送设置”。这些数据结构、下载器适配器和发送链路可以复用，但不能继续照搬 PTD 的居中推送对话框；面向用户的选择器按 PTPP 原版重做。
- PTD 的目录属于下载器，不与站点绑定；本项目只在其上增加 `siteId → downloader/profile` 映射层。
- PTD 当前只能导入 PTPP 用户历史数据，不能导入站点、下载器、目录、收藏等配置。本项目的完整迁移器是升级 PTPP 所必需的兼容能力。
- PTD 搜索和用户信息使用可配置并发队列，默认并发为 1；官方建议根据机器性能调到 5–10，过大会触发浏览器同 tab 并发限制。
- PTD 已完成高级筛选、多列排序、搜索方案自定义内容和多个内建搜索入口启停。应沿用这些实现。
- issue #327 的“完成”是允许编辑搜索方案中的请求内容，仍不是恢复 PTPP 任意动态 JS 插件；本项目继续禁止可执行脚本，只接受可验证、可序列化的请求配置。
- PTD 的媒体服务器、下载器任务总览、原生通信桥、额外备份服务、Ask AI 等 PTPP 没有的产品功能，不进入当前版本的导航、设置或验收范围。

### 基础架构与数据模型阶段验收（已完成）

本阶段是完整 P0 产品之前的可构建底座，验收边界如下：

- [x] 仓库根目录切换为 MV3 项目，旧 MV2 项目完整归档到 `legacy-mv2/`；Chrome MV3 输出到 `dist-chrome/`。
- [x] 建立强类型、带版本号的数据模型、分域 storage envelope、结构校验器和显式 `vN → vN+1` 迁移注册表。
- [x] 建立纯函数 PTPP storage 迁移；输出与输入无可变对象共享，迁移成功后保留全部旧 key。
- [x] 保留下载服务器地址/认证、站点默认下载器、每站每下载器多目录/标签和全局回退结构。
- [x] 建立 Chrome service worker、offscreen document、alarms 和类型化消息边界。
- [x] 新 MV3 bundle 不导入旧 `window.PTBackgroundService`、动态站点脚本或 MV2 background。
- [x] storage 使用不可变 revision 分片：完整写入并回读验证后才提交 metadata marker，保留一个回滚代际并拒绝缺失/错版分片。
- [x] 自动验证覆盖类型检查、迁移确定性/引用隔离、域名冲突、状态结构、损坏分片、未知 schema、构建产物、动态代码扫描、worker 生命周期、消息路由、alarms 和 offscreen 消息。

统一验证命令：`pnpm verify`。

本阶段不冒充“可替代旧 PTPP”的完整扩展：旧 UI/content/search/download 业务接线、凭据迁移提示、跨扩展 ID 的 ZIP/JSON 导入器和浏览器实机回归仍属于后续阶段。

### PTD 应用框架导入阶段（已完成）

- [x] 以 PT-depiler 提交 `9dd9ee1e10f4f2633c886f265a7aad0e1c52cab6` 为基线，将 Vue 3、Vite、Vuetify、Pinia 应用导入 `app/`。
- [x] 接入 options UI、content script、Chrome service worker、offscreen document 和类型化消息入口。
- [x] 导入 PTD 的应用框架和可供重合功能使用的静态 TypeScript 包；导入源码不代表对外启用 PTD 独有产品功能。
- [x] 10 个指定站点定义和 qBittorrent、Transmission 下载器均进入生产 bundle。
- [x] Chrome 生产输出切换为 `dist-chrome/`，CI 使用 Node.js 24 并上传该目录。
- [x] 修补依赖中的 `new Function`，完整产物扫描不含 `unsafe-eval`、`eval()`、`new Function()` 或远程脚本。
- [x] 自动验证覆盖 727 个构建文件的 manifest、入口、资源和所需站点 bundle。
- [x] 在 Chrome 中加载 `dist-chrome/`，验证设置页、内容工具栏和 service worker 启动。

本阶段完成的是应用框架和静态模块接入；下列 P0 条目只有在真实账号、站点和下载器环境中通过后才会勾选。

### PTPP 迁移与站点下载配置接线阶段（已完成自动验证）

- [x] service worker 启动时将 PTPP 版本化迁移结果幂等合并到 PTD `metadata`，已有 PTD 配置优先且日志不输出凭据。
- [x] 迁移 10 个目标站点、PTD 支持的下载器、地址/认证/超时、全局默认下载器、站点下载配置和 WebDAV。
- [x] 站点编辑页支持默认下载器、每下载器候选/默认目录、候选/默认标签和自动开始策略。
- [x] 建立统一下载目标解析器，并接入搜索结果、站点列表页、详情页、高级批量选择和右键菜单。
- [x] 跨站批量默认推送按每条种子的 `siteId` 独立解析目标；配置不完整时回到手动选择。
- [x] 自动测试覆盖运行时合并、类型标准化、地址与 WebDAV 保留、目标解析、多候选歧义和同 revision 幂等。
- [x] 用户历史合并到 PTD `userInfo`，最新记录合并到 `metadata.lastUserInfo`；体积、状态、时间和旧字段名完成转换。
- [x] 搜索快照同时写入 PTD snapshot 元信息和内容存储，并转换 PTPP/PTD 相反的 `url` / `link` 语义。
- [x] 辅种任务进入 PTD `keepUploadTask`，保留下载器、目录、暂停/自动开始和任务项。
- [x] 下载历史以稳定迁移键写入 PTD IndexedDB；保留旧版重复记录，同时保证重启和新版 revision 不会重复导入。
- [x] 运行时迁移完成标记升级到 bridge v2，并在 storage 与 IndexedDB 全部成功后才提交；任一数据域失败会安全重试。
- [x] 在加载 `dist-chrome/` 的 Chrome 中验证站点编辑 UI、service worker 启动和真实下载器推送。

### PTPP 界面外壳、站点工具栏与推送交互恢复（已完成）

本阶段先纠正当前版本过度沿用 PTD 产品界面的问题，不扩展 PTPP 原版没有的业务功能。完成后再继续“我的数据”、搜索、收藏、辅种和 WebDAV 等 P0 页面接线。

- [x] 用 PTPP 原版常驻纵向工具栏替换内容脚本的 PTD 球形 `speed-dial`；进入受支持页面后无需点击图标即可看到可用操作。
- [x] 详情页工具栏恢复“一键下载 / 下载到… / 复制链接 / 添加或移除收藏 / 默认下载器可用空间”等 PTPP 既有项目，并在不改变原有操作的前提下增加 PTD 已实现的“快捷搜索”。
- [x] “一键下载/推送到默认下载器”保持单击直推，不打开居中确认框；执行中、成功和失败状态在工具栏原位置反馈。
- [x] “下载到…/推送到…”恢复 PTPP 的按钮旁菜单：详情页、站点列表页、搜索结果、高级批量、右键链接入口和下载历史重新推送均已接入。菜单逐项显示下载服务器、地址和保存目录，点选一项立即推送。
- [x] 修正站点目录对全局默认下载器的优先级：站点仅在 qBittorrent 下存在有效目录绑定时，即使全局默认是 Transmission，也必须使用 qBittorrent 和该站点目录；不能静默落到 Transmission 根目录。
- [x] 将当前已接入 options 页面中的顶栏、左侧导航、内容标题、表格、表单和对话框恢复为 PTPP 风格；复用 PTD 业务实现的页面不得继续暴露 PTD 的卡片式/折叠式产品界面。
- [x] 导航显隐由持久化配置单向驱动，不再允许 Vuetify 抽屉内部状态回写抵消左上角按钮；按钮等待配置恢复完成后再切换并串行保存，避免初始化覆盖首次点击。48px 原生圆形按钮将 hover、focus 与 pressed 反馈限制在圆形绘制区域内，不再依赖可能产生方形 overlay 的 Vuetify 导航按钮。
- [x] 按 `usable_function/` 建立首轮视觉与交互回归基线，已覆盖“我的数据、搜索结果、下载服务器、站点设置、下载目录设置、常规设置和辅种任务”；尚未接入的收藏等 P0 页面在实现时继续按同一基线验收。
- [x] 当前阶段仅在 Chrome 中实机验证；不在 Edge 中加载测试版本，避免与用户现有旧版环境冲突。
- [x] 为工具栏默认展开、菜单目标排序、站点目录优先级和无歧义直推补充自动回归测试，再用真实 qBittorrent/Transmission 各完成至少一次实机推送。

2026-08-10 实机验收证据：Chrome reload `dist-chrome/` 后，详情页常驻工具栏和按钮旁菜单正常；菜单按“站点专用目标 → 其他通用目标”排序，并分行完整显示服务器、地址与目录。一键推送实际完成于站点绑定的 qBittorrent 独立目录；下载历史中也保留了手动选择 Transmission 根目录的已完成记录。下载历史详情只展示安全摘要，不再渲染下载 URL、请求头、Cookie、Authorization 或 passkey。“我的数据、搜索结果、下载服务器、站点设置、下载目录设置、常规设置和辅种任务”已逐页对照老板原版与 `usable_function/` 完成首轮视觉验收；搜索结果的固定操作栏、明暗模式选中高亮、安全标签配色，以及辅种基准种子与可编辑保存路径均已通过 Chrome reload 检查。左上角导航按钮已完成“隐藏 → 刷新仍隐藏 → 显示 → 换页仍显示 → 刷新仍显示”的闭环，点击反馈保持圆形。完整 `pnpm verify` 通过并验证 732 个 MV3 产物文件。

### 我的收藏完整接线（已完成）

- [x] 恢复 PTPP 风格的收藏列表、横向分组卡片、固定工具栏、筛选、排序、分页、编辑、批量选择和清理流程。
- [x] 建立版本化收藏仓库和后台 CRUD；支持分组新增、改名、改色、删除、默认分组以及收藏移入/移出分组，计数和失效引用会自动修复。
- [x] 搜索结果和详情页复用同一收藏状态；收藏页重新下载和“推送到…”复用站点绑定下载器/目录优先规则。
- [x] 收藏、分组和默认分组进入本地/WebDAV 备份、当前 MV3 恢复以及旧版独立 `collection.json` 导入。
- [x] 自动回归覆盖收藏去重、CRUD、分组级联、备份 round-trip 和 worker 消息；Chrome 实机确认旧收藏展示及分组创建、改名、改色、默认和删除流程。
- [x] 第二轮按老板版修正分组条语义：全部收藏都未分组且不存在自定义分组时不再重复显示“全部 / 未分组”；“未分组”只有在它是“全部”的真子集时才出现。
- [x] 第二轮恢复老板版收藏行：封面或原版占位图、影片信息、种子标题/副标题和分组标签合并在标题列；来源、大小、收藏时间和操作继续使用独立列。
- [x] 分组卡展示说明并保持紧凑尺寸；规范化后重复的旧收藏会无损合并分组、影片信息、缺失字段和最早收藏时间，不再直接丢弃后项。
- [x] 编辑收藏时校验 IMDb `tt数字` 与纯数字豆瓣 ID，保存时统一 IMDb 大小写，避免无效影片标识进入收藏仓库。
- [x] 收藏表格恢复 PTPP 的 12px 紧凑密度和 150px 操作列；每行严格按老板版显示“搜索/编辑 → 推送到… → 本地保存 → 删除”四项，不再混入默认推送或复制链接。
- [x] 收藏选中行使用 PTPP 明暗主题高亮，固定工具栏只更新当前筛选/分组内的删除计数；不再显示 PTD 式批量推送工具组，隐藏选中项不会被误删。
- [x] 分组卡操作按钮保持单行对齐，并仅保留老板版“推送到… / 复制链接”；收藏和分组编辑窗口只在保存成功后关闭，复制链接解析失败时一定退出 loading 并展示具体错误。
- [x] 新增收藏或修改 IMDb/豆瓣 ID 时自动抓取标题、副标题、封面、年份和影片页链接；豆瓣 ID 按老板版优先获取，网络失败仍保存收藏且不会抹掉已有影片信息。
- [x] 收藏搜索正确区分 `imdb|<ID>` 与 `douban|<ID>`，仅有豆瓣 ID 时不再错误回退到种子标题搜索。

2026-08-11 第二轮 Chrome 实机验收证据：`dist-chrome/` reload 后保留 3 条旧收藏和 86.02 GiB 数据；全部收藏未分组时不显示冗余分组条。临时分组创建后说明正常显示，将第一条收藏加入该分组时分组卡即时变为“全部 3 / 未分组 2 / 临时分组 1”，标题列同步显示分组标签；移出并删除临时分组后恢复为 3 条全部未分组，分组条再次隐藏。编辑窗口对 IMDb `123` 和豆瓣 ID `abc` 同时显示格式错误并禁用保存，随后取消，未改写真实收藏。原版影片占位图、标题/副标题/分组同列、独立来源/大小/收藏时间/操作列均通过深色模式检查；未执行“清空”，未触发真实下载。导航按钮同时完成隐藏、刷新保持、显示、换页保持和刷新保持闭环，点击反馈为圆形。

2026-08-11 导航回归复验：Chrome 将本仓库 `dist-chrome/` 从 `v2.0.0.2127+3396d17` reload 到 `v2.0.0.2128+af3509e` 构建后，左上角按钮完成“隐藏状态 → 显示 → 隐藏 → 再显示 → 页面刷新仍显示”的闭环。按钮按压截图显示 48px 圆形反馈，没有方形 overlay；最终恢复用户原有的导航展开状态，未修改站点、下载器、收藏或其他业务数据。

2026-08-11 最新用户回归反馈曾重新打开导航项：验收不能只检查持久化字段或源码字符串，必须在 Chrome 中确认导航抽屉与主内容宽度同步变化、连续显示/隐藏均有效、刷新后保留最终状态，并以截图确认 hover/pressed/ripple 全程为圆形。随后直接复核当前 `v2.0.0.2129+f4b171d` 构建，已确认点击后导航消失且主内容同步扩展，再次点击完整恢复，刷新后仍保持展开；动作截图捕获到 48px 圆形 ripple，没有方形按压层。

2026-08-11 最新 Chrome 回归：将本仓库 `dist-chrome/` 从 `v2.0.0.2130+904b0b4` reload 到工作树构建 `v2.0.0.2131+64c7ed0` 后，导航完成“隐藏状态启动 → 显示 → 再隐藏 → 刷新仍隐藏 → 最终恢复显示”的闭环，抽屉和主内容宽度同步变化；新按钮截图中的 hover/pressed 反馈是完整圆形，不再出现方形 overlay。随后以 `Fate` 搜索结果勾选两条，批量“收藏”在按钮下方打开老板版锚定菜单，并在当前没有自定义分组时正确提供“新建分组”；点击菜单外关闭，未创建分组、未新增收藏。最终收藏仍为原有 3 条、合计 86.02 GiB，未执行下载、删除、清空或真实批量收藏。

2026-08-11 收藏页最终 Chrome 验收证据：本仓库最新 `dist-chrome/` reload 后，三条 U2 收藏和 86.02 GiB 数据保持不变。12px 紧凑表格的每行只显示“编辑 / 推送到… / 本地保存 / 删除”四个老板版动作；勾选第一条时深色高亮正常，固定工具栏仅将删除计数从 0 更新为 1，未出现 PTD 式批量推送栏。取消勾选后计数恢复为 0；编辑窗口包含标题、副标题、IMDb ID、豆瓣 ID，随后取消。未触发真实下载、删除、清空或收藏数据写入。影片信息自动补全、豆瓣 ID 搜索和抓取失败回退由纯函数、消息接线和生产 worker 自动回归覆盖；完整 `pnpm verify` 通过并验证 732 个 MV3 产物文件。

当前 P0 主闭环进度：下载延迟已移除 service worker `sleep` 与固定 30 秒错误，改为精确到期时间、持久化存储、顶层 alarms 监听和 worker 启动恢复的一次性任务；用户数据失败重试也进入同一可恢复队列。搜索快照与下载历史的“清空全部”已完成实现和自动回归，仍需在 Chrome 中确认按钮、禁用态和取消确认框的最终 UI 闭环。下一阶段优先完成浏览器重启、worker 回收与休眠唤醒实机验收，然后补齐 WebDAV 刷新后备份、失败重试和状态展示。

2026-08-11 本阶段自动化证据：持久化一次性任务测试覆盖 5/30/60 秒精确到期、worker 重建恢复、过期任务唤醒后立即调度、提前 alarm 纠正、重复 alarm 去重、执行期间同 ID 新代任务保护、领取前被替换的旧代任务隔离，以及 alarm 创建失败后的存储清理；延迟下载必须等待持久化成功后才返回，关闭下载历史时以独立任务 ID 避免并发覆盖。搜索快照“清空全部”覆盖元数据与内容同步清理、无关 metadata 保留和部分写入失败回滚；下载历史“清空全部”接入既有 IndexedDB clear 消息、确认、禁用态、加载态和成功/失败反馈。完整 `pnpm verify` 通过：两套类型检查、7 组测试、生产构建、732 个 MV3 产物校验和 worker runtime smoke 均成功。此证据不替代仍未完成的 Chrome UI 与浏览器生命周期实机验收。

2026-08-11 最新用户回归发现：`fate` 搜索实际返回 651 条，但上一轮结果筛选词 `Fujimaru Ritsuka` 被保留，表格因此只显示 6 条；快照批量删除后 Vuetify 仍保留已不存在的选中 ID；下载历史和收藏等页混排启用/禁用按钮时，Vuetify 移除禁用按钮 elevation 造成视觉高度不一致。修复后每次全新搜索立即清除文本、站点、标签和范围筛选，取消旧排队任务，并以搜索代际隔离已经发出的旧请求，防止旧结果写入新搜索或快照；单项失败重试仍保留当前筛选。快照删除完成后同步剔除已删除选中 ID。全局顶栏固定 36px 高度/最大高度、8px 对齐间距，并让禁用按钮保留与启用按钮相同的阴影占位。完整 `pnpm verify` 再次通过：两套类型检查、8 组测试、生产构建、732 个 MV3 产物校验和 worker runtime smoke 均成功；筛选清零及快照删除后选中态随后由用户确认正常，最终视觉与导航实机证据见下方复核记录。

2026-08-11 搜索大结果集与顶栏第二轮修正：搜索结果每页选项限制为 10/25/50，持久化的 100/“全部”会在配置恢复时迁移到 50；行选中判断由逐行线性扫描改为共享 `Set`。标题/标签单元格不再为每一行建立三个 `ResizeObserver`，收藏状态也从每一行一次 service-worker/storage 查询合并为每次收藏版本一次共享查询，降低 651 条结果下切回标签页时的布局和消息抖动。所有 `NavButton` 不分颜色和禁用态使用同一静态基线与阴影；辅种“如何使用？”改为与收藏页一致的圆圈问号。完整 `pnpm verify` 通过两套类型检查、8 组测试、生产构建、733 个 MV3 产物校验及 worker smoke；Chrome 逐页视觉、大结果集切页和连续导航均已在下方最终复核中完成。

2026-08-11 顶栏蒙层与搜索导航实机复核：上一轮将 `NavButton` 设为 `position: static`，导致 Vuetify 绝对定位的 `.v-btn__overlay` 脱离按钮并扩张覆盖整张内容卡片；这既是下载历史、收藏、辅种、下载服务器、站点、搜索方案和参数备份页灰白蒙层的直接原因，也会干扰页面交互。组件已恢复为统一的 `position: relative` 定位容器，并以 smoke 断言禁止 `position: static` 回归。Chrome reload 后逐页确认蒙层消失，红/绿/蓝及启用/禁用按钮保持同一 36px 边界、顶边、底边和阴影基线；651 条 `Fate` 搜索中及完成后均可直接切换到下载历史等导航页。快捷站点/标签计数另由“每个按钮各扫一次全部结果”改成一次遍历生成两个计数表，减少大结果集增量更新和重新进入页面时的重复计算。完整 `pnpm verify` 再次通过两套类型检查、8 组测试、生产构建、733 个 MV3 产物校验及 worker smoke。

## 2. P0：首个可用 MV3 版本必须保留

### 2.1 实际使用的 10 个站点

以下站点必须同时支持用户信息、搜索、种子详情/列表解析和下载链接获取：

- [ ] Audiences
- [ ] Azusa / 梓喵
- [ ] HDKylin
- [ ] HDSky
- [ ] HDTime
- [ ] KamePT
- [ ] M-Team
- [ ] PTTime
- [ ] SkyeySnow
- [ ] U2

核对结果：以上 10 个站点的 typed definition 已进入当前生产 bundle，不需要搬运老版动态解析脚本。站点仍保持未勾选，表示尚未分别完成“用户信息、搜索、列表、详情、下载链接”五项真实账号闭环，而不是站点定义尚未接入。

验收标准：

- [x] 可以单独启停“参与搜索”和“获取个人信息”。
- [x] 能维护站点 URL、名称、分组、排序、超时和站点必要凭据。
- [x] 域名变化后以稳定 `siteId` 迁移配置，不能丢失下载目录映射。
- [x] 搜索或个人信息请求失败时，单站失败不阻塞其他站点。

### 2.2 我的数据

- [x] 一键刷新全部或选中站点的用户数据。
- [x] 显示站点、用户名、等级、上传量、下载量、分享率、发布数、做种数、做种体积、魔力/积分、时魔、加入时间、数据更新时间和状态。
- [x] 支持列显示、筛选、排序、每页数量和站点优先级。
- [x] 保留历史数据、时间线和统计图。
- [ ] 自动刷新用户数据必须使用 `chrome.alarms`，浏览器重启或 service worker 被回收后仍能恢复（`flushUserInfo` Alarm 已在 worker 回收和完整浏览器重启后重新出现；真实刷新执行与休眠唤醒仍待验收）。
- [x] 支持设定每天允许刷新时间、失败重试次数和重试间隔。
- [x] 搜索与用户信息队列并发数分别可配置，设置中提示推荐范围 5–10，并对过大值给出警告。

截图中的当前使用值仅作为迁移测试样本，不作为代码默认值：每日约 22:00、失败重试 5 次、间隔 5 分钟。

### 2.3 完整搜索工作流

- [x] 聚合搜索、单站搜索、搜索方案、搜索分类、标签、排序和筛选采用 PT-depiler 的同类底层实现，并使用 PTPP 页面结构。
- [x] 搜索结果保留站点、标题/副标题、标签、分类、大小、上传/下载/完成/评论数和发布时间。
- [x] 支持 Free、50%、2xFree、2xUp 等优惠标签快速过滤。
- [x] 支持批量勾选、复制链接、本地下载、远程推送、创建快照、收藏和辅种。
- [x] 默认每站结果数量可配置；迁移时保留当前值 50。
- [x] 保存最近搜索词。
- [x] 允许在页面选中文本后发起搜索。
- [x] 每次全新搜索立即清空上一轮结果筛选和待执行队列；已经发出的旧请求以代际标识隔离，不得把旧关键词结果写入新搜索或覆盖快照。
- [x] 搜索结果每页仅提供 10/25/50，旧 100/“全部”设置自动降至 50；大结果集不再为每行建立尺寸监听、重复读取完整收藏状态，快捷筛选计数也只遍历结果一次。
- [ ] IMDb 编号搜索时展示影片和评分信息（ID 搜索链路已接入，影片/评分展示仍需完整实机验收）。
- [ ] 输入关键词时展示影片候选，候选数量可配置；迁移时保留当前值 5（配置和候选链路已有，待完整实机验收）。
- [x] 搜索结果快照支持创建、加载、重命名、删除和清空；Chrome 已完成创建 → 列表 → 改名 → 加载 → 选中删除 → 清空取消 → 清空确认闭环，删除后按钮立即禁用，元数据和内容存储均清零。
- [ ] 搜索结果快照随备份导出后恢复的完整闭环仍待验收。
- [x] 沿用 PTD #475 的数据化搜索方案自定义能力，允许调整请求参数和精准/模糊搜索模式，但不得执行用户脚本。

### 2.4 下载服务器

首发必须完整支持用户当前使用的：

- [x] qBittorrent
- [x] Transmission

若直接复用 PT-depiler 下载器模块，则同时保留 Deluge、Flood、ruTorrent、Synology Download Station、µTorrent 和 Aria2，但它们不应阻塞首发。

每个下载服务器必须支持：

- [x] 自定义名称。
- [x] 启用/禁用。
- [x] 类型。
- [x] 服务器地址。
- [x] 用户名、密码或必要令牌。
- [x] 连接测试和清晰错误信息。
- [x] 请求超时；迁移时保留当前 30000ms。
- [ ] 编辑、删除和排序（编辑、删除已实现；排序仍需核查/补齐）。
- [x] 设置全局默认下载服务器。

安全要求：凭据不写入日志、截图、代码或仓库；备份中是否包含凭据必须明确提示，并支持加密。

### 2.5 最重要差异功能：站点 → 下载服务器 → 保存目录/标签

PT-depiler issue #454 最终加入了全局默认下载器，但明确拒绝“站点与下载器/目录强绑定”。本项目必须保留该绑定能力。

实现基线采用 PTD 现有能力：每个下载器的 `suggestFolders` / `suggestTags`、全局 `defaultDownloader`、快速推送以及记住上一次选择；新增的只有站点映射、回退顺序和站点优先交互。

建议的数据语义：

```text
站点下载配置
├─ siteId
├─ defaultDownloaderId
└─ downloaderProfiles
   ├─ downloaderId A
   │  ├─ defaultFolder
   │  ├─ folders[]
   │  └─ tags[]
   └─ downloaderId B
      ├─ defaultFolder
      ├─ folders[]
      └─ tags[]
```

硬性验收标准：

- [x] 同一下载服务器可以为不同站点配置不同保存目录（自动测试、代码验证及 Chrome 实机推送已验收）。
- [x] 同一站点可以在不同下载服务器下配置不同目录（自动测试、代码验证及 Chrome 历史记录已验收）。
- [x] 每个“站点 + 下载服务器”允许一个或多个候选目录，而不是只保存一个字符串。
- [x] 可以为站点指定默认下载服务器和默认目录。
- [x] 支持全局默认下载服务器与目录作为未配置站点的回退。
- [x] 支持标签，并允许站点专用标签。
- [x] 搜索结果、站点列表页、详情页和右键菜单使用同一套解析规则。
- [x] 一键推送按 `站点显式目录绑定 → 单一站点目录绑定 → 全局默认配置/下载器默认行为 → 弹出选择菜单` 回退；只有一个站点目录绑定但未显式设置站点默认下载器时，该绑定仍作为不可拆分的下载器/目录目标。
- [x] 站点目标无歧义时可无弹窗直接推送；多下载器或多目录但未指定默认值时回到选择菜单。标签只是已解析目标的附属参数；多个候选标签没有默认值时不阻断一键推送，默认不附加标签。
- [x] 高级推送对话框中，站点专用配置排在通用配置之前；最终构建的 Chrome 菜单已确认“站点专用目标 → 分隔线 → 通用目标”，首项匹配站点绑定下载器及目录。
- [x] 用户随时可以展开其他下载器、目录和标签，不能因默认设置而失去选择能力。
- [x] 下载前清楚显示最终下载服务器、目录和标签。
- [x] 下载历史保存实际使用的服务器、地址标识、目录和标签。
- [x] 站点域名变化时，绑定基于 `siteId` 而不是裸 host，避免配置丢失。
- [x] 站点下载配置进入 PTD 本地 metadata、WebDAV 可选 metadata 备份和 PTPP→MV3 数据迁移（真实备份恢复待验收）。

“默认下载器”在站点页面中的解析顺序必须固定为：

1. 站点显式指定的默认下载服务器及其站点专用目录/标签，但仅选择下载服务器而没有站点目录不构成绑定；
2. 若站点只在一个启用的下载服务器下存在有效目录绑定，该下载服务器与目录作为原子目标，隐式成为站点默认；单独存在的 `defaultDirectory` 也是有效目录绑定；
3. 若站点在多个下载服务器下存在目录绑定但没有显式默认值，要求用户选择，不能猜测或静默丢弃站点目录；
4. 只有站点完全没有目录绑定时，才回退到全局默认下载服务器及其全局目录/标签；仅有标签而没有目录不阻止回退；
5. 仍无法得到唯一目标时显示选择菜单。

已存在但不可用的站点目录绑定（下载器缺失、停用或排除当前站点）不得静默回退到全局默认下载器；应提示用户修复绑定或从“下载到…”菜单手动选择。

删除仍被站点目录绑定引用的下载器前，确认框必须列出受影响站点。只有用户确认删除并级联移除这些绑定后，相关站点才允许按全局默认规则回退；单纯停用下载器必须保留绑定并阻止静默回退。

回归案例：全局默认是 Transmission，某站点只配置了 `qBittorrent → /downloads/该站点`。该站点的“一键下载/推送到默认下载器”必须发送到 qBittorrent 的 `/downloads/该站点`，不得发送到 Transmission 的 `/downloads`。

下载目标决策矩阵：

| 站点配置                                         | 一键下载结果                                                       | 是否允许静默回退       |
| ------------------------------------------------ | ------------------------------------------------------------------ | ---------------------- |
| 没有站点独立目录；即使只选择了某个站点下载器     | 使用全局默认下载器及其正常默认目录；没有默认目录时使用下载器根目录 | 是                     |
| 只有一个可用的“下载器 + 站点目录”绑定            | 使用该绑定，忽略全局默认下载器                                     | 不适用                 |
| 同一下载器有多个站点目录，并设置了有效默认目录   | 使用该下载器及其默认站点目录                                       | 不适用                 |
| 同一下载器有多个站点目录，但没有有效默认目录     | 要求用户选择                                                       | 否                     |
| 多个下载器各有站点目录，并指定了有效站点默认绑定 | 使用该下载器及其默认站点目录                                       | 不适用                 |
| 多个下载器各有站点目录，但没有有效站点默认绑定   | 要求用户选择                                                       | 否                     |
| 已绑定下载器缺失、停用或排除当前站点             | 明确报错并打开/提示“下载到…”选择                                   | 否                     |
| 用户主动从“下载到…”菜单选择目标                  | 严格使用当次选择的下载器、目录和标签                               | 这是显式覆盖，不是回退 |

跨站批量推送必须逐条按所属站点解析，并在发送前完成整批预检；任一条存在歧义或失效绑定时先阻止整批发送并列出待处理项目，避免只发送一部分后造成状态难以判断。

建议交互：

- 普通点击：按默认配置直接推送。
- 展开按钮或长按：显示站点专用配置、其他下载器和其他目录。
- 搜索结果批量推送：默认继承每条结果所属站点的配置；跨站批量时允许预览分组后的实际去向。

### 2.6 下载设置与下载历史

- [x] 保存每次一键发送的下载历史。
- [x] 记录来源站点、标题、下载器、保存目录、标签、成功/失败、错误信息和时间。
- [x] 按下载器、成功/失败和关键词过滤。
- [x] 支持重新下载、重新推送、删除选中项和清空；Chrome 已完成重新下载选择框、单项/选中删除、清空取消和清空确认闭环，删除后选中态与按钮立即复位。
- [x] 下载失败重试可配置；当前用户配置为关闭。重试等待进入持久化 Alarm，不依赖 service worker 计时器，失败原因经脱敏后写入历史。
- [x] 批量任务可选择前台逐项执行或后台执行并通知；当前用户配置为前台。后台批次的游标、结果和计数持久化，已完成无真实下载器的 Chrome 完成闭环。
- [x] 批量下载链接间隔可配置；当前用户配置为 0 秒。前台与后台均按项目完成后的间隔串行推进，纯函数回归覆盖 0 秒和非零间隔。
- [x] 保留批量种子总体积超限确认；迁移时保留当前阈值 10 GiB。Chrome 用 11 GiB 安全夹具确认只弹一次并在取消后不新增历史、Alarm 或后台批次。
- [x] 推送完成提示在标签页处于后台时不得静默丢失；后台批次完成使用 `chrome.notifications` 持久通知并设置 `requireInteraction`，Chrome 已确认通知实际创建。

2026-08-12 下载策略与 MV3 生命周期实机证据：最终 `dist-chrome/` 由 Chrome for Testing `151.0.7922.138` 加载，扩展身份为 `PT-Plugin-Plus 2.0.0.2135+a4c1405`、Manifest V3，options、offscreen 和 service worker 目标均存在。隔离备份 profile 保留 10 个站点、2 个下载器和站点目录映射；测试全程未输出 Cookie、下载器地址或认证信息，也未触碰真实下载器。缺失下载器安全夹具验证失败状态与脱敏错误信息写入 IndexedDB，历史详情不包含 URL、请求对象、Cookie 或 Authorization。后台批次以已完成历史作为安全跳过项，自然完成为成功 1/失败 0、释放种子负载并创建 Chrome 通知。持久化下载任务在强制回收 service worker 后由 `ping` 唤醒并保留 1 个任务/1 个 Alarm，完整关闭并重启同一隔离 Chrome profile 后仍为 1/1；最终让无效任务自然失败后任务、Alarm、历史、批次、通知和快照夹具均已精准清理。完整 `pnpm verify` 通过两套类型检查、10 组业务测试、生产构建、735 个 MV3 产物校验和 worker runtime smoke。

### 2.7 我的收藏

截图中存在实际收藏数据，因此该功能升级为 P0，不能因 PT-depiler 缺失而移除。

- [x] 从搜索结果和站点页面添加/移除收藏。
- [x] 保存标题、副标题、来源站点、大小、下载链接、详情链接、影片信息和添加时间；老板原版没有独立备注字段，本轮不额外引入。
- [x] 收藏列表筛选、排序、编辑、删除和清空。
- [x] 创建、改名、改色、删除收藏分组，并可设置默认分组。
- [x] 收藏移入/移出分组；删除分组时级联清理引用并自动重算数量。
- [x] 从收藏重新下载或推送到下载服务器，并沿用站点绑定下载器/目录优先级。
- [x] 收藏进入本地/WebDAV 备份、当前 MV3 恢复和旧数据迁移。
- [x] 仅有未分组收藏时隐藏冗余分组条；存在自定义分组或未分组为真子集时按老板版显示对应卡片。
- [x] 收藏行恢复封面/占位图与标题、影片信息、分组标签同列布局；分组说明可见，工具栏在选择前后不发生明显位移。
- [x] 重复旧收藏无损合并字段、分组和最早收藏时间；编辑表单拒绝格式错误的 IMDb/豆瓣 ID。
- [x] 新增收藏或修改 IMDb/豆瓣 ID 时自动补全影片资料，失败不阻塞保存；收藏搜索按 IMDb/豆瓣 ID 生成对应搜索语法。
- [x] 收藏行只保留“搜索/编辑、推送到、本地保存、删除”，分组卡只保留“推送到、复制”；勾选后仅更新删除计数，不出现 PTD 批量操作栏。
- [x] 搜索结果批量收藏恢复老板版的“选择现有分组 / 当场新建分组”锚定菜单；明确选择的分组优先于默认分组，已收藏项目不会被批量操作意外移除。
- [x] 从 IMDb/豆瓣 ID 搜索结果收藏时继承当前搜索上下文；站点结果自带的影片 ID 始终优先，仅为缺失字段补入当前及老板旧版搜索语法中的 ID。
- [x] 批量收藏成功后发布 options 页内收藏版本变更，所有可见搜索结果行重新读取收藏状态；较早发出的异步查询不得覆盖较新的结果。已收藏项恢复老板版粉色实心爱心，默认收藏分组也使用老板版爱心语义，不再显示 PTD 式星标。
- [x] Chrome reload 最新 `dist-chrome/` 后，以临时搜索结果完成“单条收藏 → 批量收藏到临时分组 → 行级爱心同步 → 取消收藏”的闭环，并在结束前恢复原有 3 条收藏和无自定义分组状态；不得触发下载、删除既有收藏或清空。

2026-08-10 实机验收证据：Chrome 中恢复出 3 条旧收藏及 86.02 GiB 汇总；临时分组完成创建、改名、改色、设为默认和删除，删除后收藏数量、未分组数量和默认引用均恢复。未执行“清空”，也未触发真实远程下载任务。

2026-08-11 第三轮代码审计：老板版在每次收藏变更后会统一刷新收藏链接，当前 MV3 原先只更新触发操作的 `ActionTd` 实例，因此批量收藏后各行可能继续显示空心爱心。现已增加页面内共享收藏 revision，单条或批量变更会刷新所有行；异步查询使用递增请求号防止旧响应回写。新增同步信号单元测试并纳入 `pnpm test`，源码/产物 smoke test 同时锁定共享刷新、异步防串线、实心爱心与默认分组爱心图标；随后以 Chrome + Orca 完成实物闭环。

2026-08-11 第三轮 Chrome + Orca 实物回归：Chrome reload 本仓库 `dist-chrome/` 工作树构建 `v2.0.0.2132+f3efced` 后，先确认原有 3 条收藏、合计 86.02 GiB 且无自定义分组。`Fate` 搜索完成后以 `Fujimaru Ritsuka` 过滤，三条既有收藏均显示老板版粉色实心爱心和“取消收藏”；单条收藏 U2 `65302` 后当前行无刷新即时更新。再将 Audiences `656867` 与 M-Team `1196743` 批量收藏到 `Codex临时测试-20260811`，两行同时变为粉色实心爱心，切换收藏页再返回后仍保持“取消收藏”，较晚的共享状态没有被旧异步查询覆盖。收藏页正确显示“全部 6 / 160.28 GiB、未分组 4 / 110.31 GiB、临时分组 2 / 49.97 GiB”。获得清理授权后，仅取消收藏 U2 `65302`、Audiences `656867`、M-Team `1196743`，并在临时分组显示 0 条后删除 `Codex临时测试-20260811`；最终收藏页恢复为原有 3 条、合计 86.02 GiB、无自定义分组。全程未触发下载、未执行“清空”、未删除三条既有收藏，Chrome 缩放保持 100%。完整 `pnpm verify` 已通过类型检查、收藏同步/迁移/备份测试、生产构建、732 个 MV3 产物验证与 worker smoke test。

### 2.8 辅种任务

- [x] 从搜索结果创建辅种任务。
- [x] 保存来源站点、标题、大小、创建时间和校验状态。
- [x] 校验 torrent 文件信息，避免错误辅种。
- [x] 发送全部或选中辅种到下载服务器。
- [x] 删除、清空、更新、备份和恢复任务。
- [x] 在使用辅种前明确提醒下载服务器关闭“自动开始下载”。

### 2.9 WebDAV 备份与自动备份

用户实际只使用 WebDAV，因此 WebDAV 是 P0；其他服务是继承项，不阻塞首发。

- [x] 添加、编辑、删除和测试 WebDAV 服务。
- [x] 手动备份、查看历史、恢复和删除远端备份（代码已接入，真实服务器完整 round-trip 仍列入 6.4 验收）。
- [x] 可选备份：配置、站点、下载器、站点目录映射、用户数据、搜索快照、下载历史、收藏、辅种任务和 Cookies。
- [x] 支持备份加密；当前配置为不加密，但需要保留开关。
- [x] 固定间隔自动备份使用 alarms/job scheduler，不依赖常驻后台页或 `setInterval`。
- [ ] 自动备份支持固定间隔和“用户数据刷新成功后备份到指定 WebDAV”两种触发方式。
- [ ] 自动备份失败进入日志，并按策略重试；不能悄悄失效。
- [ ] 页面显示最后成功备份时间、最后失败原因和下一次计划时间。
- [ ] service worker 重启后自动重建备份任务（模块加载时已有调度注册，待浏览器重启、worker 回收和休眠唤醒实机验收）。

PT-depiler 已有 WebDAV、`backupInterval` 和 alarms 自动备份实现，可以直接借用其架构并补齐“刷新后上传”语义。

### 2.10 页面工具栏和快速推送

- [x] 站点列表页和详情页显示助手按钮/工具栏。
- [x] 工具栏默认常驻展开，外形、宽度、文字标签、分隔线、顶部黄色拖动条、空闲/悬停透明度和按钮排列对齐 PTPP 原版；不得以需要再次点击的 PTD 球形按钮作为默认形态。
- [x] 详情页默认排列从上到下为：插件 Logo、一键下载、下载到…、复制链接、添加/移除收藏、快捷搜索、默认下载器可用空间；除新增的“快捷搜索”外不改变原版层级。
- [ ] 工具栏位置可配置为页面左侧或右侧；迁移时保留当前右侧（当前支持保存拖动位置，尚无明确的左/右侧设置入口）。
- [x] 工具栏可拖动，并保留双击拖动条重置位置的 PTPP 行为；位置按站点保存且不会因窗口尺寸变化移出可视区。
- [x] 支持拖拽链接到工具栏并按站点默认配置推送。
- [x] 列表页支持批量复制、批量本地下载、批量远程推送和高级勾选。
- [x] 详情页支持复制链接、默认配置直推、按钮旁选择其他配置、收藏、默认下载器可用空间和快速搜索。
- [x] 默认推送的交互目标是“一次点击完成”，不能强制把鼠标移动到页面中央再确认。
- [x] “下载到…/推送到…”菜单以站点专用目录为第一组，其后用分隔线列出其他启用下载服务器及通用目录；详情页、站点列表页、搜索结果与批量入口均已接入。每项显示 `服务器名称 → 地址 → 目录`，选择后立即执行。
- [x] 标签等新版底层能力使用菜单项融入旧式流程；普通“推送到…”不再退回 PTD 居中大对话框，高级参数对话框只由明确的高级入口打开。
- [x] 工具栏按钮具有执行中、成功、失败的就地状态；错误信息可读且不会破坏站点页面布局。

### 2.11 PTPP options 界面与导航还原

所有 P0 功能页都以 `legacy-mv2/src/options/` 和 `usable_function/` 截图为界面基准；可以继续使用 Vue 3、Vuetify 3、Pinia 和 PTD 的底层 store/service，但不得把 PTD 页面直接当成最终界面。

- [x] 恢复 PTPP 黄色顶部栏，并对照原版恢复菜单、产品名、全局搜索框、当前搜索方案和主页/帮助入口的位置与尺寸；左上角按钮在持久化配置恢复完成后切换导航栏，换页或刷新不会意外复位，并保持圆形点击反馈。
- [x] 为导航按钮补充行为级回归约束：除持久化字段与源码约束外，Chrome 实机确认抽屉和主内容布局同步变化、连续切换有效、刷新保持状态；首次加载期间的点击不能被配置恢复覆盖，hover、focus 与 pressed 反馈均不能退回方形。
- [x] 恢复 PTPP 深色左侧导航及“概览 / 参数设置 / 鸣谢”等分组；首发只显示 PTPP 已有且本清单保留的入口。
- [x] 当前已接入页面使用 PTPP 的宽屏内容结构、蓝色页面标题条、紧凑工具按钮和全宽数据表，主要信息不再压缩在 PTD 卡片或折叠面板中。
- [x] “我的数据”恢复老板原版的一行一站表格和顶部筛选/刷新/图表工具区，并完成字段、密度、分页、明暗主题和图表入口的首轮验收。
- [x] “下载服务器”“站点设置”“下载目录设置”恢复为三个独立页面，并完成列表、启停开关和编辑入口的首轮验收。
- [x] “常规设置”恢复 PTPP 的常规/搜索/下载/高级标签结构；当前可见配置已按 PTPP 保留范围完成首轮验收。
- [x] 搜索结果、收藏和辅种继续使用 PTPP 的页面层级与术语。
- [x] 快照、下载历史、收藏、辅种、下载服务器、站点、搜索方案和参数备份/恢复已恢复 PTPP 页面层级与术语；各页顶栏使用统一 64px 桌面高度、36px 按钮/紧凑输入框、8px 对齐间距和 14px 上下内边距。所有颜色及禁用态按钮在组件层固定同一基线、阴影和有界 overlay，并已由 Chrome 逐页复核。
- [ ] 弹窗、确认、加载、成功和错误反馈在视觉上统一为 PTPP 风格，同时保留键盘操作、可读标签和小窗口下的基本可用性。
- [ ] 逐页确认没有 `PTD`、`PT-depiler`、“我的下载器”、媒体服务器、原生桥、Ask AI 等越界名称或入口泄漏到成品界面。

### 2.12 PTPP 原有影片信息

- [ ] 保留 PTPP 已有的 IMDb/豆瓣影片信息、评分和 ID 搜索。
- [ ] 对外部数据做缓存、超时和失败回退，影片信息失败不能阻塞种子搜索。
- [x] TMDb 属于用户曾提出的候选增强，但不是 PTPP 原有功能；本轮不实现、不显示设置入口，另行确认后再排期。

## 3. P1：按 PT-depiler 重做或在 P0 稳定后补齐

- [x] 右键菜单：选中文本搜索、豆瓣/IMDb 链接搜索、下载链接推送；TMDb 不属于本轮范围。
- [x] 地址栏关键词搜索。
- [x] 深色/浅色/跟随系统主题；迁移截图使用深色主题。
- [ ] 系统日志、调试器和更新日志（系统日志及版本提示已有；PTD 调试器源码未进入当前 PTPP 导航，需按产品范围决定是否恢复并完成验收）。

## 4. P2：可延后或首版移除

这些功能没有出现在实际使用清单中，不应阻塞 MV3：

| 功能                             | 建议                                                              |
| -------------------------------- | ----------------------------------------------------------------- |
| 自定义语言包导入/替换            | 首版移除；以后可作为纯数据功能恢复。                              |
| Chrome storage.sync 旧式直接备份 | 移除；由本地备份和 WebDAV 取代。                                  |
| GoodMoviesList 页面增强          | 移除。                                                            |
| 封面模式                         | 移除；以后有明确需求再以卡片视图重做。                            |
| `reseed.tongyifan.me` 页面增强   | 移除，除非确认仍在使用。                                          |
| 任意自定义站点插件 JS/CSS        | 移除；MV3 不允许延续任意动态代码执行。                            |
| 任意可执行搜索入口脚本           | 移除；保留 PTD #475 的数据化请求配置和内建 typed search entries。 |
| 独立 schema 管理页               | 移除；schema 改为源码类型和构建期注册。                           |
| 独立权限设置页                   | 可移除；改为功能触发时请求最小必要权限。                          |
| OWSS                             | 非必需；WebDAV 稳定后再决定。                                     |
| 老版捐赠、团队、技术栈等静态页面 | 不阻塞首发，可简化。                                              |
| PTD 媒体服务器/媒体库            | 当前移除入口；PTPP 原版没有该产品功能。                           |
| PTD“我的下载器”任务总览          | 当前移除入口；只保留 PTPP 下载服务器设置和推送能力。              |
| PTD 原生通信桥                   | 当前移除入口；不属于 PTPP MV3 升级所需功能。                      |
| PTD 额外备份服务                 | 当前不显示；本阶段只实现用户实际使用的 WebDAV。                   |
| PTD Ask AI 与额外影片来源        | 当前移除入口；另行明确需求后再评估。                              |

## 5. 数据迁移清单

- [x] 读取 `PT-Plugin-Plus-Config`（自动迁移基础已实现）。
- [x] 为 10 个实际站点建立 depiler `siteId` 静态映射；未知/自定义站点生成稳定本地 ID。
- [x] 迁移 qBittorrent、Transmission 等旧下载器的地址和认证配置。
- [x] 迁移 `client.paths[host]` 为新的站点下载配置，并保留每个路径数组及顺序。
- [x] 迁移默认下载服务器和各站点默认下载服务器。
- [x] 迁移下载历史（不按 URL 再次去重，失败记录也保留），并接入 PTD IndexedDB。
- [x] 迁移用户历史数据，将 host 键映射为 `siteId`，并接入 PTD `userInfo` / `lastUserInfo`。
- [x] 迁移收藏项目和收藏分组，重算分组计数。
- [x] 迁移搜索结果快照，为结果追加 `siteId`，并接入 PTD 快照列表与内容存储。
- [x] 迁移辅种任务，保留下载器、目录和任务明细，并接入 PTD 辅种任务存储。
- [x] 迁移 WebDAV/OWSS 服务器设置。
- [x] 自动刷新、重试、批量下载阈值和页面工具栏等旧选项已保留在版本化配置；后续业务 UI 再接入强类型字段。
- [ ] 对凭据迁移显示明确提示；不在日志中输出原文。
- [x] 纯迁移函数可重复执行且结果确定；自动迁移成功后仍不删除任何旧 key。
- [x] 新扩展 ID 无法直接读取旧扩展 storage 时，普通恢复入口可自动识别并迁移完整 PTPP ZIP；独立 `userdatas.json` 仍可按需补充。

说明：`PT-Plugin-Plus-Cache-Contents` 是可重新生成的页面/API 运行缓存，本阶段有意不迁移；它不是用户配置或历史数据。旧动态站点/下载器脚本也只保留在未删除的 MV2 key 中，不进入 MV3 可消费状态。

## 6. 技术升级清单

### 6.1 MV3 基础

- [x] 根项目采用 `manifest_version: 3` 构建，旧 MV2 源码保存在 `legacy-mv2/`。
- [x] `browser_action` 改为 `action`。
- [x] 新 MV3 Chrome background 使用 service worker。
- [ ] Firefox 使用独立兼容 background 构建。
- [x] 权限拆分为 `permissions`、`optional_permissions` 和 `host_permissions`。
- [x] Web Accessible Resources 使用 MV3 对象格式声明内容脚本所需图标、库和样式。
- [x] 新 MV3 manifest 和完整生产 bundle 不含 `unsafe-eval`、`eval()` 或 `new Function()`。

### 6.2 后台与任务

- [x] Chrome API 监听器在 service worker 顶层同步注册。
- [x] 已建立 offscreen document 的幂等创建及 DOMParser/剪贴板消息入口；旧解析器迁移仍待完成。
- [x] 已接入 PTD 类型化消息协议、options UI 和内容脚本，并完成站点默认下载器/目录/标签差异功能的首轮业务接线。
- [x] 新数据模型和迁移结果进入版本化 `chrome.storage.local`；大数据域是否转 IndexedDB 待容量测试决定。
- [x] 用户刷新、下载延迟/重试、自动备份全部使用 alarms/job scheduler；下载延迟和用户数据失败重试使用持久化的一次性任务队列，保留精确到期时间，不再依赖 service worker `sleep` 或固定 30 秒。
- [x] 持久化一次性下载、站点间隔、失败重试和后台批次可在 service worker 被回收及完整浏览器重启后恢复；Chrome 已验证任务、游标与 Alarm 不丢失，已完成历史不会重复推送。
- [ ] 自动用户刷新、固定间隔 WebDAV 备份及全部任务仍需完成系统休眠唤醒后的实机验证。

### 6.3 消灭动态代码

- [x] 生产构建使用 PTD 静态 TypeScript 下载器类，不加载旧下载器 `init.js`。
- [x] 10 个指定站点使用 PT-depiler typed definitions 并进入生产 bundle。
- [x] 从 MV3 生产构建移除所有 `eval` 和 `new Function`，完整产物通过动态代码扫描。
- [x] MV3 生产构建不下载并执行远端脚本。
- [x] MV3 生产构建只接受静态模块与数据配置，不执行远端配置代码。

### 6.4 验证

- [x] Chrome for Testing `151.0.7922.138` 已完成最终 MV3 构建加载、service worker 回收和完整浏览器重启测试。
- [ ] Chrome 当前稳定版加载及系统休眠唤醒测试。
- [ ] Edge 当前稳定版兼容性回归（不在当前开发环境加载，不阻塞 Chrome 首发）。
- [ ] Firefox 若列入首发则独立测试。
- [ ] 10 个站点逐站搜索、用户信息、列表、详情和下载回归。
- [x] qBittorrent、Transmission 连接和推送回归。
- [ ] 每个站点的默认目录实机验证。
- [ ] 跨站批量推送验证实际目录不会串站。
- [ ] WebDAV 手动/自动备份、恢复、断网重试验证。
- [ ] MV2 备份导入和升级后数据完整性验证。
- [ ] Chrome Web Store 远程代码、权限和隐私政策检查。

## 7. 首发完成定义

首个 MV3 版本只有同时满足以下条件才能称为“可替代当前 PTPP”：

1. 10 个实际站点的搜索和用户信息可用。
2. qBittorrent、Transmission 可连接和推送。
3. 每个站点都能自动落到用户指定的下载服务器与目录，且无需每次重复选择。
4. 下载历史、收藏、搜索快照、辅种任务没有数据损失。
5. WebDAV 手动与自动备份可验证成功，并能恢复。
6. 自动用户数据刷新和失败重试在 MV3 service worker 生命周期下可靠运行。
7. 老版配置可以通过自动迁移或导入工具进入新版。

### 当前剩余工作的推荐顺序

1. 补齐 WebDAV 的“用户数据刷新成功后备份”、失败重试、最后成功/失败原因与下一次计划时间，再做真实服务器手动/自动备份、恢复和断网闭环。
2. 对自动用户刷新、固定间隔 WebDAV 备份及持久化任务做系统休眠唤醒验收；Chrome for Testing 的 worker 回收和完整浏览器重启已通过。
3. 对 10 个目标站点逐站完成用户信息、搜索、列表、详情、下载链接和默认目录验证，并做跨站批量推送防串目录验证。
4. 完成搜索快照随备份恢复、MV2 ZIP 数据完整性和 WebDAV 恢复后的历史/收藏/辅种/站点目录核对。
5. 完成 IMDb/豆瓣影片候选与评分、凭据迁移提示、全部 PTPP 弹窗/反馈风格及成品越界名称审计。
6. 完成 Chrome 稳定版与 Chrome Web Store 权限/远程代码/隐私政策检查；Edge/Firefox 兼容性在 Chrome 首发后单独安排。
