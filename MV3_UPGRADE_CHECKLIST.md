# PT-Plugin-Plus Manifest V3 升级范围与验收清单

更新日期：2026-08-10
依据：`usable_function/` 中的实际使用截图、PT-Plugin-Plus 现有代码、PT-depiler 当前实现，以及 PT-depiler issue [#454](https://github.com/pt-plugins/PT-depiler/issues/454)。

## 1. 已确定的产品方向

- 站点定义、站点设置、完整搜索能力优先仿照 PT-depiler 的数据模型和交互，不继续维护 PTPP 的动态 JS 站点插件体系。
- MV3 架构参考 PT-depiler：Chrome service worker + offscreen document + 类型化消息 + alarms + storage/IndexedDB。
- 保留 PTPP 最有价值的差异能力：**按站点绑定下载服务器、保存目录和标签，并能快速直推**。
- 第一版只为实际使用功能负责；未使用的旧功能不阻塞 MV3 首发。
- 配置迁移必须保留用户现有站点、下载器、站点目录映射、历史、收藏、快照和 WebDAV 设置。

### PT-depiler 官方计划与迁移 FAQ 的最终核对

2026-08-10 核对了 PT-depiler 的[开发计划](https://github.com/orgs/pt-plugins/projects/1/views/1)和[迁移常见问题 #316](https://github.com/pt-plugins/PT-depiler/discussions/316)：

- PTD 已完成默认下载器、下载器候选目录/标签、快速推送和“记住上一次推送设置”。这些能力直接复用，不重新造基础下载器选择器。
- PTD 的目录属于下载器，不与站点绑定；本项目只在其上增加 `siteId → downloader/profile` 映射层。
- PTD 当前只能导入 PTPP 用户历史数据，不能导入站点、下载器、目录、收藏等配置。本项目的完整迁移器属于明确新增能力。
- PTD 搜索和用户信息使用可配置并发队列，默认并发为 1；官方建议根据机器性能调到 5–10，过大会触发浏览器同 tab 并发限制。
- PTD 已完成高级筛选、多列排序、搜索方案自定义内容和多个内建搜索入口启停。应沿用这些实现。
- issue #327 的“完成”是允许编辑搜索方案中的请求内容，仍不是恢复 PTPP 任意动态 JS 插件；本项目继续禁止可执行脚本，只接受可验证、可序列化的请求配置。
- 开发计划当前 P0 包含站点适配和辅种任务，P1 包含统一查看下载服务器种子列表；相关实现成熟时优先复用。

### 基础架构与数据模型阶段验收（已完成）

本阶段是完整 P0 产品之前的可构建底座，验收边界如下：

- [x] 仓库根目录切换为 MV3 项目，旧 MV2 项目完整归档到 `legacy-mv2/`；Chrome MV3 输出到 `dist-chrome/`。
- [x] 建立强类型、带版本号的数据模型、分域 storage envelope、结构校验器和显式 `vN → vN+1` 迁移注册表。
- [x] 建立纯函数 PTPP storage 迁移；输出与输入无可变对象共享，迁移成功后保留全部旧 key。
- [x] 保留下载服务器地址/认证、站点默认下载器、每站每下载器多目录/标签和全局回退结构。
- [x] 建立 Chrome service worker、offscreen document、alarms 和类型化消息边界。
- [x] 新 MV3 bundle 不导入旧 `window.PTBackgroundService`、动态站点脚本或 MV2 background。
- [x] storage 使用不可变 revision 分片：完整写入并回读验证后才提交 metadata marker，保留一个回滚代际并拒绝缺失/错版分片。
- [x] 自动验证覆盖类型检查、迁移确定性/引用隔离、域名冲突、状态结构、损坏分片、未知 schema、构建产物、动态代码扫描、worker 生命周期、消息路由、alarms 和 offscreen 消息。

统一验证命令：`pnpm verify`。

本阶段不冒充“可替代旧 PTPP”的完整扩展：旧 UI/content/search/download 业务接线、凭据迁移提示、跨扩展 ID 的 ZIP/JSON 导入器和浏览器实机回归仍属于后续阶段。

### PTD 应用框架导入阶段（已完成）

- [x] 以 PT-depiler 提交 `9dd9ee1e10f4f2633c886f265a7aad0e1c52cab6` 为基线，将 Vue 3、Vite、Vuetify、Pinia 应用导入 `app/`。
- [x] 接入 options UI、content script、Chrome service worker、offscreen document 和类型化消息入口。
- [x] 导入 PTD 的站点、搜索、下载器、备份、社交/影片信息等静态 TypeScript 包。
- [x] 10 个指定站点定义和 qBittorrent、Transmission 下载器均进入生产 bundle。
- [x] Chrome 生产输出切换为 `dist-chrome/`，CI 使用 Node.js 24 并上传该目录。
- [x] 修补依赖中的 `new Function`，完整产物扫描不含 `unsafe-eval`、`eval()`、`new Function()` 或远程脚本。
- [x] 自动验证覆盖 753 个构建文件的 manifest、入口、资源和所需站点 bundle。
- [ ] 在 Chrome/Edge 中加载 `dist-chrome/`，验证设置页、内容工具栏和 service worker 启动。

本阶段完成的是应用框架和静态模块接入；下列 P0 条目只有在真实账号、站点和下载器环境中通过后才会勾选。

### PTPP 迁移与站点下载配置接线阶段（已完成自动验证）

- [x] service worker 启动时将 PTPP 版本化迁移结果幂等合并到 PTD `metadata`，已有 PTD 配置优先且日志不输出凭据。
- [x] 迁移 10 个目标站点、PTD 支持的下载器、地址/认证/超时、全局默认下载器、站点下载配置和 WebDAV。
- [x] 站点编辑页支持默认下载器、每下载器候选/默认目录、候选/默认标签和自动开始策略。
- [x] 建立统一下载目标解析器，并接入搜索结果、站点列表页、详情页、高级批量选择和右键菜单。
- [x] 跨站批量默认推送按每条种子的 `siteId` 独立解析目标；配置不完整时回到手动选择。
- [x] 自动测试覆盖运行时合并、类型标准化、地址与 WebDAV 保留、目标解析、多候选歧义和同 revision 幂等。
- [x] 用户历史合并到 PTD `userInfo`，最新记录合并到 `metadata.lastUserInfo`；体积、状态、时间和旧字段名完成转换。
- [x] 搜索快照同时写入 PTD snapshot 元信息和内容存储，并转换 PTPP/PTD 相反的 `url` / `link` 语义。
- [x] 辅种任务进入 PTD `keepUploadTask`，保留下载器、目录、暂停/自动开始和任务项。
- [x] 下载历史以稳定迁移键写入 PTD IndexedDB；保留旧版重复记录，同时保证重启和新版 revision 不会重复导入。
- [x] 运行时迁移完成标记升级到 bridge v2，并在 storage 与 IndexedDB 全部成功后才提交；任一数据域失败会安全重试。
- [ ] 在加载 `dist-chrome/` 的 Chrome/Edge 中验证站点编辑 UI、service worker 启动和真实下载器推送。

## 2. P0：首个可用 MV3 版本必须保留

### 2.1 实际使用的 10 个站点

以下站点必须同时支持用户信息、搜索、种子详情/列表解析和下载链接获取：

- [ ] Audiences
- [ ] Azusa / 梓喵
- [ ] HDKylin
- [ ] HDSky
- [ ] HDTime
- [ ] KamePT
- [ ] M-Team
- [ ] PTTime
- [ ] SkyeySnow
- [ ] U2

核对结果：以上 10 个站点在 PT-depiler 当前源码中均有定义，可以采用其 typed site definition，不需要搬运老版动态解析脚本。

验收标准：

- [ ] 可以单独启停“参与搜索”和“获取个人信息”。
- [ ] 能维护站点 URL、名称、分组、排序、超时和站点必要凭据。
- [ ] 域名变化后以稳定 `siteId` 迁移配置，不能丢失下载目录映射。
- [ ] 搜索或个人信息请求失败时，单站失败不阻塞其他站点。

### 2.2 我的数据

- [ ] 一键刷新全部或选中站点的用户数据。
- [ ] 显示站点、用户名、等级、上传量、下载量、分享率、发布数、做种数、做种体积、魔力/积分、时魔、加入时间、数据更新时间和状态。
- [ ] 支持列显示、筛选、排序、每页数量和站点优先级。
- [ ] 保留历史数据、时间线和统计图。
- [ ] 自动刷新用户数据必须使用 `chrome.alarms`，浏览器重启或 service worker 被回收后仍能恢复。
- [ ] 支持设定每天允许刷新时间、失败重试次数和重试间隔。
- [ ] 搜索与用户信息队列并发数分别可配置，设置中提示推荐范围 5–10，并对过大值给出警告。

截图中的当前使用值仅作为迁移测试样本，不作为代码默认值：每日约 22:00、失败重试 5 次、间隔 5 分钟。

### 2.3 完整搜索工作流

- [ ] 聚合搜索、单站搜索、搜索方案、搜索分类、标签、排序和筛选采用 PT-depiler 实现。
- [ ] 搜索结果保留站点、标题/副标题、标签、分类、大小、上传/下载/完成/评论数和发布时间。
- [ ] 支持 Free、50%、2xFree、2xUp 等优惠标签快速过滤。
- [ ] 支持批量勾选、复制链接、本地下载、远程推送、创建快照、收藏和辅种。
- [ ] 默认每站结果数量可配置；迁移时保留当前值 50。
- [ ] 保存最近搜索词。
- [ ] 允许在页面选中文本后发起搜索。
- [ ] IMDb 编号搜索时展示影片和评分信息。
- [ ] 输入关键词时展示影片候选，候选数量可配置；迁移时保留当前值 5。
- [ ] 搜索结果快照支持创建、加载、重命名、删除、清空和备份恢复。
- [ ] 沿用 PTD #475 的数据化搜索方案自定义能力，允许调整请求参数和精准/模糊搜索模式，但不得执行用户脚本。

### 2.4 下载服务器

首发必须完整支持用户当前使用的：

- [ ] qBittorrent
- [ ] Transmission

若直接复用 PT-depiler 下载器模块，则同时保留 Deluge、Flood、ruTorrent、Synology Download Station、µTorrent 和 Aria2，但它们不应阻塞首发。

每个下载服务器必须支持：

- [ ] 自定义名称。
- [ ] 启用/禁用。
- [ ] 类型。
- [ ] 服务器地址。
- [ ] 用户名、密码或必要令牌。
- [ ] 连接测试和清晰错误信息。
- [ ] 请求超时；迁移时保留当前 30000ms。
- [ ] 编辑、删除和排序。
- [ ] 设置全局默认下载服务器。

安全要求：凭据不写入日志、截图、代码或仓库；备份中是否包含凭据必须明确提示，并支持加密。

### 2.5 最重要差异功能：站点 → 下载服务器 → 保存目录/标签

PT-depiler issue #454 最终加入了全局默认下载器，但明确拒绝“站点与下载器/目录强绑定”。本项目必须保留该绑定能力。

实现基线采用 PTD 现有能力：每个下载器的 `suggestFolders` / `suggestTags`、全局 `defaultDownloader`、快速推送以及记住上一次选择；新增的只有站点映射、回退顺序和站点优先交互。

建议的数据语义：

```text
站点下载配置
├─ siteId
├─ defaultDownloaderId
└─ downloaderProfiles
   ├─ downloaderId A
   │  ├─ defaultFolder
   │  ├─ folders[]
   │  └─ tags[]
   └─ downloaderId B
      ├─ defaultFolder
      ├─ folders[]
      └─ tags[]
```

硬性验收标准：

- [x] 同一下载服务器可以为不同站点配置不同保存目录（自动测试/代码验证；实机推送待验收）。
- [x] 同一站点可以在不同下载服务器下配置不同目录（自动测试/代码验证；实机推送待验收）。
- [x] 每个“站点 + 下载服务器”允许一个或多个候选目录，而不是只保存一个字符串。
- [x] 可以为站点指定默认下载服务器和默认目录。
- [x] 支持全局默认下载服务器与目录作为未配置站点的回退。
- [x] 支持标签，并允许站点专用标签。
- [x] 搜索结果、站点列表页、详情页和右键菜单使用同一套解析规则。
- [x] 一键推送按 `站点专用配置 → 全局默认配置/下载器默认行为 → 弹出选择框` 回退。
- [x] 站点目标无歧义时可无弹窗直接推送；多目录或多标签但未指定默认值时回到选择框。
- [ ] 高级推送对话框中，站点专用配置排在通用配置之前。
- [ ] 用户随时可以展开其他下载器、目录和标签，不能因默认设置而失去选择能力。
- [ ] 下载前清楚显示最终下载服务器、目录和标签。
- [ ] 下载历史保存实际使用的服务器、地址标识、目录和标签。
- [x] 站点域名变化时，绑定基于 `siteId` 而不是裸 host，避免配置丢失。
- [x] 站点下载配置进入 PTD 本地 metadata、WebDAV 可选 metadata 备份和 PTPP→MV3 数据迁移（真实备份恢复待验收）。

建议交互：

- 普通点击：按默认配置直接推送。
- 展开按钮或长按：显示站点专用配置、其他下载器和其他目录。
- 搜索结果批量推送：默认继承每条结果所属站点的配置；跨站批量时允许预览分组后的实际去向。

### 2.6 下载设置与下载历史

- [ ] 保存每次一键发送的下载历史。
- [ ] 记录来源站点、标题、下载器、保存目录、标签、成功/失败、错误信息和时间。
- [ ] 按下载器、成功/失败和关键词过滤。
- [ ] 支持重新下载、重新推送、删除选中项和清空。
- [ ] 下载失败重试可配置；当前用户配置为关闭。
- [ ] 批量任务可选择前台逐项执行或后台执行并通知；当前用户配置为前台。
- [ ] 批量下载链接间隔可配置；当前用户配置为 0 秒。
- [ ] 保留批量种子总体积超限确认；迁移时保留当前阈值 10 GiB。
- [ ] 推送完成提示在标签页处于后台时不得静默丢失；回到标签页后应能看到结果，或提供持久通知中心。

### 2.7 我的收藏

截图中存在实际收藏数据，因此该功能升级为 P0，不能因 PT-depiler 缺失而移除。

- [ ] 从搜索结果和站点页面添加/移除收藏。
- [ ] 保存标题、副标题、来源站点、大小、下载链接、详情链接、影片信息、添加时间和备注。
- [ ] 收藏列表筛选、排序、编辑、删除和清空。
- [ ] 创建、改名和删除收藏分组。
- [ ] 收藏移入/移出分组。
- [ ] 从收藏重新下载或推送到下载服务器。
- [ ] 收藏进入本地/WebDAV 备份和旧数据迁移。

### 2.8 辅种任务

- [ ] 从搜索结果创建辅种任务。
- [ ] 保存来源站点、标题、大小、创建时间和校验状态。
- [ ] 校验 torrent 文件信息，避免错误辅种。
- [ ] 发送全部或选中辅种到下载服务器。
- [ ] 删除、清空、更新、备份和恢复任务。
- [ ] 在使用辅种前明确提醒下载服务器关闭“自动开始下载”。

### 2.9 WebDAV 备份与自动备份

用户实际只使用 WebDAV，因此 WebDAV 是 P0；其他服务是继承项，不阻塞首发。

- [ ] 添加、编辑、删除和测试 WebDAV 服务。
- [ ] 手动备份、查看历史、恢复和删除远端备份。
- [ ] 可选备份：配置、站点、下载器、站点目录映射、用户数据、搜索快照、下载历史、收藏、辅种任务和 Cookies。
- [ ] 支持备份加密；当前配置为不加密，但需要保留开关。
- [ ] 自动备份必须使用 `chrome.alarms`，不能依赖常驻后台页或 `setInterval`。
- [ ] 自动备份支持固定间隔和“用户数据刷新成功后备份到指定 WebDAV”两种触发方式。
- [ ] 自动备份失败进入日志，并按策略重试；不能悄悄失效。
- [ ] 页面显示最后成功备份时间、最后失败原因和下一次计划时间。
- [ ] service worker 重启后自动重建备份任务。

PT-depiler 已有 WebDAV、`backupInterval` 和 alarms 自动备份实现，可以直接借用其架构并补齐“刷新后上传”语义。

### 2.10 页面工具栏和快速推送

- [ ] 站点列表页和详情页显示助手按钮/工具栏。
- [ ] 工具栏位置可配置为页面左侧或右侧；迁移时保留当前右侧。
- [ ] 支持拖拽链接到工具栏并按站点默认配置推送。
- [ ] 列表页支持批量复制、批量本地下载、批量远程推送和高级勾选。
- [ ] 详情页支持复制链接、默认配置直推、展开选择其他配置和快速搜索。
- [ ] 默认推送的交互目标是“一次点击完成”，不能强制把鼠标移动到页面中央再确认。

### 2.11 TMDb 与影片信息

- [ ] 保留 IMDb/豆瓣影片信息、评分和 ID 搜索。
- [ ] 新增 TMDb 影片及电视剧支持。
- [ ] TMDb 页面可以直接用标题、IMDb ID 或 TMDb ID 搜索种子。
- [ ] 搜索候选和搜索结果可展示 TMDb 海报、年份、类型、简介、评分和评分人数。
- [ ] 支持电影、剧集、季度和单集 URL。
- [ ] 对外部数据做缓存、超时和失败回退，TMDb 失败不能阻塞种子搜索。
- [ ] 优先借用 PT-depiler 的 `packages/social/entity/tmdb.ts`。
- [ ] 可先使用 PT-depiler 现有的 TMDb 页面解析；若增加 TMDb API，则新增 API Token 设置，并避免写入日志和明文非加密备份。

## 3. P1：按 PT-depiler 重做或在 P0 稳定后补齐

- [ ] 右键菜单：选中文本搜索、豆瓣/IMDb/TMDb 链接搜索、下载链接推送。
- [ ] 地址栏关键词搜索。
- [ ] 下载器内任务查看、暂停、恢复和删除。
- [ ] 更丰富的影片来源：Bangumi、AniDB、TVmaze。
- [ ] 深色/浅色/跟随系统主题；迁移截图使用深色主题。
- [ ] 系统日志、调试器和更新日志。
- [ ] Cookie 自动延长等 PT-depiler 新功能，默认关闭。
- [ ] 更多备份服务和媒体服务器，仅在复用成本低时随架构继承。

## 4. P2：可延后或首版移除

这些功能没有出现在实际使用清单中，不应阻塞 MV3：

| 功能                             | 建议                                                              |
| -------------------------------- | ----------------------------------------------------------------- |
| 自定义语言包导入/替换            | 首版移除；以后可作为纯数据功能恢复。                              |
| Chrome storage.sync 旧式直接备份 | 移除；由本地备份和 WebDAV 取代。                                  |
| GoodMoviesList 页面增强          | 移除。                                                            |
| 封面模式                         | 移除；以后有明确需求再以卡片视图重做。                            |
| `reseed.tongyifan.me` 页面增强   | 移除，除非确认仍在使用。                                          |
| 任意自定义站点插件 JS/CSS        | 移除；MV3 不允许延续任意动态代码执行。                            |
| 任意可执行搜索入口脚本           | 移除；保留 PTD #475 的数据化请求配置和内建 typed search entries。 |
| 独立 schema 管理页               | 移除；schema 改为源码类型和构建期注册。                           |
| 独立权限设置页                   | 可移除；改为功能触发时请求最小必要权限。                          |
| OWSS                             | 非必需；WebDAV 稳定后再决定。                                     |
| 老版捐赠、团队、技术栈等静态页面 | 不阻塞首发，可简化。                                              |

## 5. 数据迁移清单

- [x] 读取 `PT-Plugin-Plus-Config`（自动迁移基础已实现）。
- [x] 为 10 个实际站点建立 depiler `siteId` 静态映射；未知/自定义站点生成稳定本地 ID。
- [x] 迁移 qBittorrent、Transmission 等旧下载器的地址和认证配置。
- [x] 迁移 `client.paths[host]` 为新的站点下载配置，并保留每个路径数组及顺序。
- [x] 迁移默认下载服务器和各站点默认下载服务器。
- [x] 迁移下载历史（不按 URL 再次去重，失败记录也保留），并接入 PTD IndexedDB。
- [x] 迁移用户历史数据，将 host 键映射为 `siteId`，并接入 PTD `userInfo` / `lastUserInfo`。
- [x] 迁移收藏项目和收藏分组，重算分组计数。
- [x] 迁移搜索结果快照，为结果追加 `siteId`，并接入 PTD 快照列表与内容存储。
- [x] 迁移辅种任务，保留下载器、目录和任务明细，并接入 PTD 辅种任务存储。
- [x] 迁移 WebDAV/OWSS 服务器设置。
- [x] 自动刷新、重试、批量下载阈值和页面工具栏等旧选项已保留在版本化配置；后续业务 UI 再接入强类型字段。
- [ ] 对凭据迁移显示明确提示；不在日志中输出原文。
- [x] 纯迁移函数可重复执行且结果确定；自动迁移成功后仍不删除任何旧 key。
- [ ] 新扩展 ID 无法直接读取旧扩展 storage 时，提供 PTPP ZIP/JSON 导入器。

说明：`PT-Plugin-Plus-Cache-Contents` 是可重新生成的页面/API 运行缓存，本阶段有意不迁移；它不是用户配置或历史数据。旧动态站点/下载器脚本也只保留在未删除的 MV2 key 中，不进入 MV3 可消费状态。

## 6. 技术升级清单

### 6.1 MV3 基础

- [x] 根项目采用 `manifest_version: 3` 构建，旧 MV2 源码保存在 `legacy-mv2/`。
- [x] `browser_action` 改为 `action`。
- [x] 新 MV3 Chrome background 使用 service worker。
- [ ] Firefox 使用独立兼容 background 构建。
- [x] 权限拆分为 `permissions`、`optional_permissions` 和 `host_permissions`。
- [x] Web Accessible Resources 使用 MV3 对象格式声明内容脚本所需图标、库和样式。
- [x] 新 MV3 manifest 和完整生产 bundle 不含 `unsafe-eval`、`eval()` 或 `new Function()`。

### 6.2 后台与任务

- [x] Chrome API 监听器在 service worker 顶层同步注册。
- [x] 已建立 offscreen document 的幂等创建及 DOMParser/剪贴板消息入口；旧解析器迁移仍待完成。
- [x] 已接入 PTD 类型化消息协议、options UI 和内容脚本，并完成站点默认下载器/目录/标签差异功能的首轮业务接线。
- [x] 新数据模型和迁移结果进入版本化 `chrome.storage.local`；大数据域是否转 IndexedDB 待容量测试决定。
- [ ] 用户刷新、下载延迟/重试、自动备份全部使用 alarms/job scheduler。
- [ ] 所有任务可在 service worker 被回收后恢复。

### 6.3 消灭动态代码

- [x] 生产构建使用 PTD 静态 TypeScript 下载器类，不加载旧下载器 `init.js`。
- [x] 10 个指定站点使用 PT-depiler typed definitions 并进入生产 bundle。
- [x] 从 MV3 生产构建移除所有 `eval` 和 `new Function`，完整产物通过动态代码扫描。
- [x] MV3 生产构建不下载并执行远端脚本。
- [x] MV3 生产构建只接受静态模块与数据配置，不执行远端配置代码。

### 6.4 验证

- [ ] Chrome 当前稳定版加载、重启、休眠唤醒测试。
- [ ] Edge 当前稳定版测试。
- [ ] Firefox 若列入首发则独立测试。
- [ ] 10 个站点逐站搜索、用户信息、列表、详情和下载回归。
- [ ] qBittorrent、Transmission 连接和推送回归。
- [ ] 每个站点的默认目录实机验证。
- [ ] 跨站批量推送验证实际目录不会串站。
- [ ] WebDAV 手动/自动备份、恢复、断网重试验证。
- [ ] MV2 备份导入和升级后数据完整性验证。
- [ ] Chrome Web Store 远程代码、权限和隐私政策检查。

## 7. 首发完成定义

首个 MV3 版本只有同时满足以下条件才能称为“可替代当前 PTPP”：

1. 10 个实际站点的搜索和用户信息可用。
2. qBittorrent、Transmission 可连接和推送。
3. 每个站点都能自动落到用户指定的下载服务器与目录，且无需每次重复选择。
4. 下载历史、收藏、搜索快照、辅种任务没有数据损失。
5. WebDAV 手动与自动备份可验证成功，并能恢复。
6. 自动用户数据刷新和失败重试在 MV3 service worker 生命周期下可靠运行。
7. 老版配置可以通过自动迁移或导入工具进入新版。
